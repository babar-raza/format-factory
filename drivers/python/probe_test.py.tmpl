# SCAFFOLD_STATUS: FORMAT_ADAPTATION_REQUIRED
# PROVENANCE: template=probe_test, pattern=E, language=python
"""Test skeleton for ${function_name}() — Pattern E (Probe)."""
import sys
from pathlib import Path
_REPO_ROOT = Path(__file__).resolve().parents[3]
sys.path.insert(0, str(_REPO_ROOT / "src" / "python"))
from ${module} import ${function_name}


class Test${format_cap}Probe:
    def test_returns_dict(self):
        # FIXTURE_REQUIRED: replace b"" with real ${format_cap} format bytes
        result = ${function_name}(b"")
        # ORACLE_REQUIRED: assert specific probe fields, not isinstance(result, dict)

    def test_format_field(self):
        # FIXTURE_REQUIRED: replace b"" with real ${format_cap} format bytes
        result = ${function_name}(b"")
        assert result.get("format") == "${format_lower}"
