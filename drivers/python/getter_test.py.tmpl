# SCAFFOLD_STATUS: FORMAT_ADAPTATION_REQUIRED
# PROVENANCE: template=getter_test, pattern=A, language=python
"""Test skeleton for ${function_name}() — Pattern A (Getter)."""
import sys
from pathlib import Path
_REPO_ROOT = Path(__file__).resolve().parents[3]
sys.path.insert(0, str(_REPO_ROOT / "src" / "python"))
from ${module} import ${function_name}


class Test${class_name}:
    def test_basic(self):
        # FIXTURE_REQUIRED: replace None with a real model or valid input object
        result = ${function_name}(None)
        # EXPECTED_VALUE_REQUIRED: replace with a meaningful assertion

    def test_returns_${return_type_safe}(self):
        # FIXTURE_REQUIRED: replace None with a real model or valid input object
        result = ${function_name}(None)
        # ORACLE_REQUIRED: assert spec-derived expected value, not isinstance(result, object)
