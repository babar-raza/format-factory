# SCAFFOLD_STATUS: FORMAT_ADAPTATION_REQUIRED
# PROVENANCE: template=export_csv_test, pattern=B, language=python
"""Test skeleton for ${function_name}() — Pattern B (ExportCsv)."""
import sys
from pathlib import Path
_REPO_ROOT = Path(__file__).resolve().parents[3]
sys.path.insert(0, str(_REPO_ROOT / "src" / "python"))
from ${module} import ${function_name}


class Test${format_cap}CsvExport:
    def test_returns_string(self):
        # FIXTURE_REQUIRED: replace b"" with real ${format_cap} format bytes
        result = ${function_name}(b"")
        # EXPECTED_VALUE_REQUIRED: assert meaningful behavior

    def test_writes_file(self, tmp_path):
        dest = tmp_path / "out.csv"
        # FIXTURE_REQUIRED: replace b"" with real ${format_cap} format bytes
        ${function_name}(b"", dest=dest)
        assert dest.exists()
        # EXPECTED_VALUE_REQUIRED: assert file content validity
