# SCAFFOLD_STATUS: FORMAT_ADAPTATION_REQUIRED
# PROVENANCE: template=append_test, pattern=D, language=python
"""Test skeleton for ${function_name}() — Pattern D (Append)."""
import sys
from pathlib import Path
_REPO_ROOT = Path(__file__).resolve().parents[3]
sys.path.insert(0, str(_REPO_ROOT / "src" / "python"))
from ${module} import ${function_name}


class Test${format_cap}Append:
    def test_append_increases_count(self):
        # FIXTURE_REQUIRED: replace b"" with real ${format_cap} format bytes
        result = ${function_name}(b"", [])
        # EXPECTED_VALUE_REQUIRED: assert count increased by exactly 1, not just key presence
        assert "${collection_key}" in result
