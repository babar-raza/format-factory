# PBM Object Model Requirements
# Generated: R73 Train K (fixture mode — no live AI call)
# Source: src/python/pbm/pbm_parser.py analysis
# Schema version: format-factory-generated-requirements v1.0
# lifecycle_stage: ai_draft
# Review required before authoritative status

schema_version: "1.0"
format_id: pbm
artifact_id: pbm-object-model-requirements
generated_at: "2026-05-29"
generator: claude-sonnet-4-6
pipeline: AI-GENERATED-FORMAT-REQUIREMENTS-PIPELINE
pipeline_mode: fixture
generation_note: "Generated by analyzing pbm_parser.py (R73). No live AI call. Fixture mode."

requirements:

  - id: PBM-ENT-001
    entity: PbmImage
    description: >
      The PbmImage entity represents a fully decoded PBM image.
      Fields: width (int), height (int), magic (str: P1|P4), pixels (list[int]),
      path (str).
    lifecycle_stage: ai_draft
    source: src/python/pbm/pbm_parser.py
    acceptance_criteria:
      - PbmImage.width and height must be positive integers
      - PbmImage.magic must be P1 or P4
      - PbmImage.pixels length must equal width * height
      - All pixel values must be 0 or 1

  - id: PBM-ENT-002
    entity: PbmPixelStats
    description: >
      Pixel statistics result from image_pixel_stats(). Added R73 Train G.
      Fields: ok (bool), black_count (int), white_count (int), total_pixels (int),
      black_density (float), width (int), height (int), magic (str).
    lifecycle_stage: ai_draft
    source: src/python/pbm/pbm_parser.py
    acceptance_criteria:
      - black_count + white_count == total_pixels
      - black_density in [0.0, 1.0]
      - Returns ok=False on parse failure (never raises)

  - id: PBM-ENT-003
    entity: PbmProbeResult
    description: >
      Result from probe_pbm(). Non-destructive header inspection.
      Fields: path (str), exists (bool), valid_header (bool), magic (str),
      width (int), height (int).
    lifecycle_stage: ai_draft
    source: src/python/pbm/pbm_parser.py
    acceptance_criteria:
      - Always returns a dict (never raises)
      - exists=False when file is missing
      - valid_header=False when magic is not P1 or P4
