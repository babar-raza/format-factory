<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- FormatFactory.Markdown — standalone .NET Markdown target writer library -->
    <!-- Sprint: FORMAT-FACTORY-DOTNET-TARGET-WRITER-MWP-DOGFOOD-UNBLOCKING-001 -->
    <!-- target_ff_library: FormatFactory.Markdown.MarkdownWriter -->
    <!-- commercial_product_ready: false — G11-G not approved -->
    <TargetFramework>net10.0</TargetFramework>
    <AssemblyName>FormatFactory.Markdown</AssemblyName>
    <RootNamespace>FormatFactory.Markdown</RootNamespace>
    <Version>0.1.0-mwp</Version>
    <Authors>format-factory</Authors>
    <Description>FormatFactory.Markdown: Standalone .NET Markdown target writer library. MWP implementation. Not release-ready.</Description>
    <PackageId>FormatFactory.Markdown</PackageId>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AllowUnsafeBlocks>false</AllowUnsafeBlocks>
    <TreatWarningsAsErrors>false</TreatWarningsAsErrors>
  </PropertyGroup>

</Project>
