[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "format-factory-nrrd"
version = "0.1.0.dev0"
description = "Format Factory NRRD (Nearly Raw Raster Data) FOSS Python parser and writer"
license = {text = "Apache-2.0"}
requires-python = ">=3.9"

readme = "README.md"
authors = [
    {name = "Format Factory", email = "formatfactory@aspose.com"},
]
keywords = ["format-factory", "nrrd", "scientific", "raster", "imaging"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent",
]

[project.urls]
Homepage = "https://github.com/format-factory/format-factory"

[project.scripts]
ff-nrrd = "nrrd.cli:main"

[tool.setuptools.packages.find]
where = [".."]
include = ["nrrd*"]
