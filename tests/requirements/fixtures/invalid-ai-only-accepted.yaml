# Fixture: invalid-ai-only-accepted.yaml
# Lane C: tests/requirements/fixtures
# Purpose: fixture where AI_PROPOSAL is ACCEPTED — must fail validation
# Schema: schemas/generated-requirements/commercial-format-requirements.schema.json
# Authority rule: AI_PROPOSAL cannot be ACCEPTED without verifier review + DEC-034 IV (AGENTS.md AF13)

format: fods
spec_version: "ODF 1.3"
generator_version: "1.0"
generation_timestamp: "2026-05-13T00:00:00Z"
model_tool: "test-fixture"
ai_available: true
product_goals_ref: "docs/commercial-product-capability-model.md"
input_source_hashes: {}
requirements:
  - requirement_id: FODS-REQ-099
    format: fods
    capability_level: C4
    requirement_type: object_model
    title: AI-proposed requirement that was incorrectly accepted
    description: This requirement was generated by AI and accepted without verifier review or DEC-034 IV. This is a governance violation.
    product_goal_mapping: [PG-001]
    source_evidence:
      - source_type: AI_PROPOSAL
        reference: ai-synthesis-session-xyz
    source_type: AI_PROPOSAL
    confidence: MEDIUM
    status: ACCEPTED
    test_requirements:
      - Test placeholder
    validation_notes: "AI_PROPOSAL cannot be ACCEPTED — must be verifier-reviewed + DEC-034 IV first"
    implementation_target: null
    known_limitations: null
