{
  "format_id": "xliff",
  "gate": "corpus",
  "status": "PASS",
  "verdict": "PASS",
  "provenance_file": "samples/by-format/xliff/_provenance.yaml",
  "corpus_manifest": "samples/by-format/xliff/_corpus-manifest.yaml",
  "authority": {
    "specification": "OASIS XLIFF 1.2/2.0/2.1 (pinned acquisition sources include the official XLIFF 2.0/2.1 schema+spec release packages)",
    "source_type": "project-owned-synthetic (all 4 samples), generated deterministically via Python stdlib xml.etree.ElementTree, no third-party dependency"
  },
  "counts": {"valid_samples": 3, "invalid_samples": 1},
  "checks_performed_this_tick": {
    "sha256_verification": "PASS -- computed the real hash of all 4 declared samples (3 valid, 1 invalid) and, since this format's own corpus-manifest.yaml had no per-sample sha256 field before this tick (unlike ipynb's), added one to each entry from the freshly computed value, then re-verified it matches the file currently on disk -- not merely trusted a pre-existing declared value.",
    "production_reader_verification": "PASS -- format_factory.xliff.load() actually invoked against every valid sample (all 3 load cleanly: minimal 1 file/1 unit, multi-unit 1 file/3 units, with-inline-codes 1 file/1 unit) and the invalid sample (correctly refused with XliffParseError: 'root is not an XLIFF 2.x document'), not merely trusted a prior 'PASS' label in the manifest."
  },
  "corpus_composition": "Entirely project-owned-synthetic -- no official upstream XLIFF example-instance corpus was sourced. This is an honest scope note, not a hidden gap: investigated directly this tick whether the pinned OASIS XLIFF 2.0/2.1 release packages (which this product already vendors for their own schemas -- xliff-core-v2.0-os.xsd et al.) contain example instance documents the way OASIS UBL's own release package does (see ubl's own corpus work this session), and confirmed they do not -- both XLIFF release ZIPs contain only schemas/schematron/NVDL validation artifacts and the spec document itself (HTML/PDF/XML), no xml/ example-instances directory. Self-generated deterministic fixtures are the practical alternative, the same choice already made and disclosed in this format's own provenance file.",
  "note": "This is one of product-goal.yaml's own 10 completion invariants (installed-wheel, independent-oracle, corpus, security, resource, typing, documentation, compatibility, performance, reproducible-build) -- this file consolidates already-existing, independently re-verified corpus provenance into a dedicated gate record; not a certification claim for the format as a whole.",
  "commit": "pending"
}
