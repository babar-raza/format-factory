{
  "format_id": "xliff",
  "gate": "documentation",
  "status": "PASS",
  "verdict": "PASS",
  "docs_present": ["README.md", "CHANGELOG.md", "MIGRATION.md", "SECURITY.md"],
  "readme_line_count": 199,
  "readme_sections": [
    "Installation", "Common lifecycle", "Segment QA checks", "Effective xml:space / xml:lang resolution",
    "Translation state transitions", "Template merge and drift detection",
    "Module coverage and schema validation", "Preservation modes", "Security and resource limits",
    "Public namespace", "Security boundary", "Current scope", "License"
  ],
  "checks_performed_this_tick": {
    "readme_substantially_rewritten": "The prior README was 27 lines, no section headers, and did not document any of this package's actual feature depth (QA checks, state transitions, template merge/drift-check, module coverage, schema validation, preservation modes, resource limits). Rewritten to 175 lines / 12 sections covering the real public API, following the exact same depth pattern already established for ipynb's own documentation gate.",
    "symbol_existence": "PASS -- every symbol referenced by a `from format_factory.xliff import ...` statement in the README (20 distinct names across 10 code blocks, +effective_attributes_by_unit this tick) actually exists on the current module, checked via hasattr, not merely assumed from the source text",
    "example_execution": "PASS -- the README's own 'Common lifecycle' example (probe/load/validate/dump) was executed verbatim against a real corpus sample (samples/by-format/xliff/valid/multi-unit.xliff) end-to-end: probed true, loaded, validated clean, and dumped to a real output file -- not merely a syntax check. This required fixing a real, honest gap found in the course of writing this example: all 3 'valid' corpus samples had `structural_test: PASS` in their own manifest (meaning `load()` succeeds) but none actually passed `validate()` -- each was missing the required `segment/@id` attribute this package's own validator enforces (xliff.segment.id.required). Fixed all 3 sample files directly (added `id=\"s1\"` to each segment) and recomputed/updated their sha256 in _corpus-manifest.yaml; all 3 now genuinely pass both load() and validate(). Full xliff suite re-run afterward confirmed zero regressions from this fix (591 passed, unchanged). The new 'Effective xml:space / xml:lang resolution' example (FF6-EVENT-000342) was executed end to end through the real XML parser (not a synthetic in-memory model construction): a hand-built but real, parseable XLIFF 2.0 document with `xml:space=\"preserve\"` on `<file>` and `xml:lang=\"fr\"` on a nested `<group>` was loaded via `load()`, then `effective_attributes_by_unit()` correctly resolved `u1` (inside the group) to `('preserve', 'fr')` and its sibling `u2` (outside the group, same file) to `('preserve', None)` -- confirming both the file-level xml:space cascade and the group-scoped xml:lang cascade against real parsed markup, not assumed from the docstring.",
    "changelog_content_accuracy": "PASS (FF6-EVENT-000349, same check dimension opened for ubl/nrrd/ora at Events 346-348) -- CHANGELOG.md had exactly 2 bullets covering only the initial namespace/lifecycle-API and bounded-parsing/typed-model milestone, with no mention of QA checks, effective xml:space/xml:lang resolution, state transitions, template merge/drift detection, module coverage/schema validation, preservation modes, or resource limits -- all real, shipped, already README-documented (this session's own prior work plus Event 342) capabilities. Appended 7 new bullets to the existing '## 0.2.0.dev0' section (pyproject.toml version unchanged, confirming no new heading was needed); all 19 cited symbols verified via hasattr against the current module before writing."
  },
  "incidental_finding": "FF6-EVENT-000342: applying the same __all__-vs-README diff technique used for safetensors (Event 340) and ipynb (Event 341) to xliff (81 exported symbols vs. ~44 previously documented via code example or explicit prose mention) surfaced several candidates beyond cosmetic supporting-type gaps: `canonicalize()` and `check_drift()` are real, standalone, undocumented functions, but each is a lower-level counterpart of an already-documented higher-level capability (`check_preservation()`/`dumps(mode=CANONICAL)` for canonicalize; `merge_with_drift_check()` for check_drift) -- judged lower priority than a capability with no documented entry point at all. `effective_attributes_by_unit()` was that capability: a complete, obligation-linked (XLIFF-MODEL-001 / SAL-XLIFF-OBL-7DA078717EA60881, SAL-XLIFF-OBL-867500AA0AC3D2C1), one-call whole-document xml:space/xml:lang resolver with no documented analog (the README's only other cascade example, length_violations, resolves a structurally different attribute). Closed that gap this tick. Remaining undocumented candidates surveyed but deliberately not combined into this slice: the 6 individual `check_*` QA functions (already reachable via `run_qa_checks(checks=[...])`, so a real but lower-severity gap), `load_xliff`/`write_xliff`/`probe_xliff` (possible aliases of `load`/`dump`/`probe`, not yet verified), `xliff_installed_workflow` (analytics/CLI entry point, parallels ipynb's own undocumented `ipynb_installed_workflow`), the `XliffError` exception hierarchy, and `bundled_module_schema_paths`/`bundled_nvdl_path`/`bundled_schematron_paths` (advanced schema-file-path accessors) -- named here so a future tick does not re-run the same __all__ extraction from scratch. FF6-EVENT-000349: closed the CHANGELOG staleness gap (opened for ubl at Event 346) for xliff -- ipynb/safetensors remain open.",
  "honest_gap": "Not every one of the README's 10 code examples was executed end-to-end this tick (the merge/drift-check and state-transition examples reference illustrative variables like `before_document`/`after_document`/`adapter`/`extracted_template` a real caller would supply) -- symbol-existence was verified for all of them via direct API/signature inspection, but only the 'Common lifecycle' and 'Effective xml:space / xml:lang resolution' examples were run against real parsed data, matching the exact scope already established for ipynb's own documentation gate.",
  "note": "This is one of product-goal.yaml's own 10 completion invariants (installed-wheel, independent-oracle, corpus, security, resource, typing, documentation, compatibility, performance, reproducible-build) -- this file records the documentation invariant only, not a certification claim for the format as a whole.",
  "regression": {
    "xliff_suite": "591 passed, 0 failed (unchanged by the README rewrite; sample-corpus fix verified separately, also 0 regressions)"
  },
  "commit": "pending"
}
