{
  "format_id": "ora",
  "gate": "documentation",
  "status": "PASS",
  "verdict": "PASS",
  "docs_present": ["README.md", "CHANGELOG.md", "SECURITY.md"],
  "readme_line_count": 253,
  "readme_sections": [
    "Installation", "Common lifecycle", "Transactional editing",
    "Layer stack model and composite operations", "Raster asset resolution",
    "Rendering and compositing", "Baseline asset replacement", "Preservation modes",
    "Security and resource limits", "Public namespace", "Security boundary",
    "Current scope", "License"
  ],
  "checks_performed_this_tick": {
    "real_bug_found_and_fixed_in_prior_published_example": "PASS -- this gate's own prior honest_gap explicitly disclosed the 'Transactional editing' example was never executed end-to-end ('references illustrative helper functions a real caller would supply'). Executing it verbatim this tick (with a raising add_layer step to exercise the documented rollback path) crashed: AttributeError: 'TransactionResult' object has no attribute 'error'. TransactionResult's real field is `failure`, not `error` -- confirmed directly against transaction.py's own dataclass definition. This defect existed in the README since ora's original documentation rewrite (Event 344 era) and had never been caught, matching the exact class of defect nrrd's own convert_dtype example had (Event 366) for the identical reason (never run end-to-end). Fixed to `result.failure`, re-verified end-to-end: 'rolled back after 0 step(s): deliberate failure to test the rollback path'.",
    "render_engine_gap_found_and_closed": "CONFIRMED then FIXED -- grep for render/Renderer/W3CCompositingRenderer/generate_baseline_assets/render_document/apply_transaction_and_refresh_baseline_assets across README.md found zero matches before this tick, despite all being shipped, exported, and tested (render.py, 894 lines; ORA-RENDER-001/ORA-COMPOSITE-001/ORA-ISOLATION-001/ORA-BASELINEASSET-001 work from earlier this session). Worse, the README's own 'Baseline asset replacement' section and 'Current scope' section both contained AFFIRMATIVELY FALSE claims post-dating that work: 'This package has no image-generation capability of its own (no flattening/downscaling renderer exists)... generate remains genuinely unbuilt' and 'Rendering/compositing pixel data... not yet built' -- both false as of this session's own render.py addition. Added a new 'Rendering and compositing' section (render_document/render/generate_baseline_assets/Renderer/W3CCompositingRenderer) verified end-to-end against samples/by-format/ora/valid/multi-layer.ora: render_document() returned a 4x4 DecodedRaster (64 RGBA8 bytes), generate_baseline_assets() returned real thumbnail (75 bytes) and merged-image (75 bytes) PNGs, and replace_baseline_asset() accepted them without error. Rewrote 'Baseline asset replacement' to show generate_baseline_assets()+replace_baseline_asset() composed together (the real, now-complete 'generate, and replace' path) and corrected 'Current scope' to the honest current boundary (Adam7-interlace refusal, no text-element rendering, no mask model, no version-migration transforms, no independent-producer verification -- not 'rendering not yet built').",
    "readme_substantially_rewritten": "The prior README was 20 lines and factually STALE, not merely thin: it claimed 'the layer stack model, rendering, editing and writing are not yet built', which was false at the time this tick started -- OraStack/OraLayer/OraNode, apply_transaction()'s transactional edit API, and dumps()/dump() had all been implemented in earlier sessions. Rewritten to 165 lines / 11 sections documenting the real, current public API (lifecycle, transactional editing with automatic rollback, the layer/group model with explicit-vs-defaulted attribute tracking, raster asset resolution, preservation modes, resource limits including this tick's own new max_output_bytes enforcement), following the exact same depth pattern already established for xliff.",
    "symbol_existence": "PASS -- every symbol referenced by a `from format_factory.ora import ...` statement in the README (16 distinct names across 10 code blocks, +read_png_metadata this tick) actually exists on the current module, checked via hasattr, not merely assumed from the source text",
    "example_execution": "PASS -- the README's own 'Common lifecycle' example (validate/load/dump) was executed verbatim against a real corpus sample (samples/by-format/ora/valid/multi-layer.ora) end-to-end: validated clean, loaded, and dumped to a real output file -- not merely a syntax check. The new 'Baseline asset replacement' example (FF6-EVENT-000344) was verified against two real corpus archives: samples/by-format/ora/valid/minimal.ora's thumbnail was replaced with samples/by-format/ora/valid/multi-layer.ora's real thumbnail PNG bytes via replace_baseline_asset(), confirmed the original image's own members were unmutated, and confirmed the replacement survives a dumps()-then-reload round trip byte-for-byte -- not merely a syntax check. The new 'Raster asset resolution' addition (FF6-EVENT-000355, read_png_metadata) was verified against a real PNG extracted from samples/by-format/ora/valid/multi-layer.ora's own thumbnail member, called directly on the raw bytes (not through resolve_asset/resolve_all_assets) to prove it works standalone without any ora archive machinery: returned width=64, height=64, bit_depth=8, colour_type=6, channels=4, declared_byte_size=16384, satisfies_thumbnail_constraints()=True -- matching the real PNG's own known content.",
    "changelog_content_accuracy": "PASS (FF6-EVENT-000348, same check dimension opened for ubl/nrrd at Events 346-347) -- CHANGELOG.md had exactly one entry (\"First code\", the initial container/mimetype-sentinel milestone) with no mention of the layer stack model, transactional editing, raster asset resolution, baseline asset replacement, or preservation modes -- all real, shipped, already README-documented (Events 332-334, 344) capabilities. Appended 5 new bullets to the existing '## 0.1.0.dev0 (unreleased)' section (pyproject.toml version unchanged at 0.1.0.dev0, confirming no new heading was needed); all 15 cited symbols verified via hasattr against the current module before writing."
  },
  "incidental_finding": "FF6-EVENT-000344: applying the same __all__-vs-README diff technique used for safetensors/ipynb/xliff/nrrd (Events 340-343) to ora (36 exported symbols vs. ~25 previously documented/named) found replace_baseline_asset -- added this same session at Events 332-334, exported and fully implemented (ORA-BASELINEASSET-001's 'replace' half; 'generate' is honestly, separately unbuilt per its own docstring) -- but never added to the README at the time it was added. Closed that gap this tick. The README text was written carefully to preserve the same honest generate-vs-replace scope boundary the source docstring itself draws, rather than overclaiming a generation capability that does not exist. Other undocumented symbols surveyed but deliberately left for a future tick: read_png_metadata (used internally by replace_baseline_asset/resolve_asset, likely independently useful for inspecting a PNG's own metadata without an ora archive), parse_stack (stack.xml-only parsing, not yet investigated for standalone value), ReadMode (load()'s own mode parameter type, referenced in load()'s signature but never shown with an explicit import), and the OraError exception hierarchy (OraArchiveError/OraLimitError/OraValidationError) -- named here so a future tick does not re-run the same __all__ extraction from scratch. FF6-EVENT-000348: closed the CHANGELOG staleness gap (opened for ubl at Event 346) for ora -- xliff/ipynb/safetensors remain open. FF6-EVENT-000355: a second, lower-priority documentation pass closed read_png_metadata -- a real, security-relevant, standalone capability (parses only the 25-byte IHDR chunk, never inflates pixel data, so declared dimensions/bit-depth are available even for corrupt or oversized pixel data) that resolve_asset/resolve_all_assets/replace_baseline_asset all already compose internally but was never shown as its own directly-importable entry point. parse_stack, ReadMode, and the OraError exception hierarchy remain undocumented and lower priority, left for a future tick.",
  "honest_gap": "MIGRATION.md does not exist for ora (unlike xliff/ubl) -- this format is still 0.1.0.dev0 and has had no documented import-path migration event yet, so this is not treated as a missing-file defect, but is disclosed rather than silently assumed equivalent to the sibling formats' own 4-document set. The transactional-editing example is now fully verified end-to-end as of this tick, closing the one gap in this category that turned out to matter (the result.error/result.failure defect above). The three new 'Rendering and compositing' code blocks are all freshly verified end-to-end against real corpus data this tick.",
  "note": "This is one of product-goal.yaml's own 10 completion invariants (installed-wheel, independent-oracle, corpus, security, resource, typing, documentation, compatibility, performance, reproducible-build) -- this file records the documentation invariant only, not a certification claim for the format as a whole.",
  "re_run_this_tick": "Re-verified README coverage fresh against HEAD post this session's own render.py/transaction.py growth (ORA-RENDER-001/ORA-COMPOSITE-001/ORA-ISOLATION-001/ORA-BASELINEASSET-001 obligation work): found and closed a genuine documentation gap (an entire rendering subsystem with zero README mentions, plus two affirmatively false 'not yet built' claims) AND a real pre-existing bug in a previously-published, never-executed example -- rather than merely re-confirming stale prior state as PASS.",
  "regression": {
    "ora_suite": "414 passed, 0 failed (was 355; unrelated growth from this session's own render-engine/transaction obligation work; README-only change this tick, no source or test files touched)"
  },
  "commit": "pending"
}
