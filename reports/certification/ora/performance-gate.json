{
  "format_id": "ora",
  "gate": "performance",
  "status": "PASS",
  "verdict": "PASS",
  "tool": "tools/certification/performance_benchmark.py",
  "baseline_file": "reports/certification/ora/performance-baseline.json",
  "results": {
    "sample": "samples/by-format/ora/valid/with-groups.ora",
    "parse_median_ms": 0.59,
    "parse_min_ms": 0.54,
    "write_median_ms": 0.13,
    "write_min_ms": 0.12,
    "memory_peak_kb": 78.8,
    "runs": 5
  },
  "genuinely_new_this_tick": "No performance benchmark existed for ora anywhere in the repository before this tick. Added benchmark_ora() following the exact same measurement pattern already established for xliff/safetensors (time.perf_counter over 5 runs for parse/write, tracemalloc for peak memory) and registered it in the tool's own main() loop. Re-ran the full tool since main() always reruns every registered benchmark; only the genuinely new ora-specific output (this file, plus performance-baseline.json) is committed -- the other formats' own baseline files were regenerated as a side effect with pure timing noise (no structural change) and were reverted rather than committed, matching the exact precedent already established when this same thing happened during xliff's own performance-gate work.",
  "re_run_this_tick": "FF6-EVENT-000439: independently re-ran, continuing the ora leg of the nrrd/ora/ubl gate sweep. ora's own baseline was already refreshed as a side effect of Event 410's shared tool invocation, left uncommitted at the time since ora was outside that tick's own ipynb-focused scope; now within this sweep's own scope. New numbers (parse=0.49ms/write=0.11ms vs prior 1.02ms/0.26ms) differ but stay within ordinary sub-millisecond timing noise for a sample this small; memory peak unchanged at 78.8KB. No SLA exists to violate in either direction.",
  "re_run_this_tick_2": "Re-ran fresh against current HEAD (post this session's own render.py/transaction.py/migration growth, Events 468-504) via .venv/Scripts/python tools/certification/performance_benchmark.py -- ran the whole tool (it has no per-format flag) and reverted the other 8 formats' own timing-noise side-effect changes via git checkout, keeping only ora's own regenerated baseline, matching the exact template already established for the FF6-EVENT-000439 re-run above and for nrrd's own performance-gate freshening earlier this session. New numbers (parse=0.59ms/write=0.13ms vs prior 0.49ms/0.11ms) stay within ordinary sub-millisecond timing noise for a sample this small on a shared machine; memory peak unchanged at 78.8KB. No SLA exists to violate in either direction.",
  "note_on_thresholds": "No pre-existing pass/fail performance threshold exists for any format in this tool -- it records baselines, it does not gate on them. This gate's own PASS verdict reflects 'a real, reproducible baseline now exists and the operations complete in reasonable time for a document this size', not a numeric SLA.",
  "note": "This is one of product-goal.yaml's own 10 completion invariants (installed-wheel, independent-oracle, corpus, security, resource, typing, documentation, compatibility, performance, reproducible-build) -- this file records the performance invariant only, not a certification claim for the format as a whole.",
  "commit": "pending"
}
