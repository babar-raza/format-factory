{
  "format_id": "safetensors",
  "gate": "corpus",
  "status": "PASS",
  "verdict": "PASS",
  "provenance_file": "samples/by-format/safetensors/_provenance.yaml",
  "corpus_manifest": "samples/by-format/safetensors/_corpus-manifest.yaml",
  "authority": {
    "specification": "https://huggingface.co/docs/safetensors/",
    "source_type": "project-owned-synthetic (all samples), generated deterministically via Python stdlib struct+json+bytes, no third-party dependency"
  },
  "counts": {"valid_samples": 3, "invalid_samples": 1},
  "checks_performed_this_tick": {
    "sha256_verification": "PASS -- FF6-EVENT-000388 found this claim was previously inaccurate: neither _corpus-manifest.yaml nor _provenance.yaml actually declared a per-sample sha256 (confirmed via direct grep, zero matches), unlike ipynb's and xliff's own manifests which do. Computed the real hash of all 4 samples (3 valid, 1 invalid) and added the declared sha256 field to each entry in _corpus-manifest.yaml this tick (the same fix already applied to xliff's own manifest previously), rather than continuing to claim a comparison against a value that did not exist.",
    "production_reader_verification": "PASS -- FF6-EVENT-000388 also found this claim was previously inaccurate: the invalid sample does NOT raise BufferError as previously stated (no such class exists anywhere in this package's own error hierarchy, errors.py defines only SafeTensorsError/SafeTensorsParseError/SafeTensorsWriteError). Directly invoked format_factory.safetensors.load() against every valid sample (all 3 load cleanly, 1/2/1 tensors respectively) and the invalid sample, which correctly raises SafeTensorsParseError('declared header extends beyond the input') -- the format's own actual, correctly-typed exception. Also corrected the manifest's own error_type field from the non-existent label 'InvalidHeaderLength' to the real exception class name."
  },
  "corpus_composition": "Entirely project-owned-synthetic -- no official upstream safetensors test corpus was sourced (unlike ipynb, which pulls 2 samples from the real nbformat GitHub repo). This is an honest scope note, not a hidden gap: the safetensors reference ecosystem (Hugging Face's own safetensors crate/PyPI package) does not ship a small, redistributable test-fixture corpus the way nbformat does, so self-generated deterministic fixtures are the practical alternative -- the same choice already made and disclosed in this format's own provenance file.",
  "note": "This is one of product-goal.yaml's own 10 completion invariants (installed-wheel, independent-oracle, corpus, security, resource, typing, documentation, compatibility, performance, reproducible-build) -- this file consolidates already-existing, independently re-verified corpus provenance into a dedicated gate record; not a certification claim for the format as a whole.",
  "commit": "pending"
}
