{
  "format_id": "safetensors",
  "gate": "documentation",
  "status": "PASS",
  "verdict": "PASS",
  "docs_present": ["README.md", "CHANGELOG.md", "SECURITY.md"],
  "readme_line_count": 103,
  "readme_sections_covered": [
    "header parsing and lazy mmap access", "reader refusal behavior enumerated",
    "validate() diagnostic codes", "borrowed-view lifecycle and close() safety",
    "full in-memory load()/dump() API", "co-installability with the official safetensors package",
    "sharded index lifecycle", "optional numpy/torch adapters"
  ],
  "checks_performed_this_tick": {
    "symbol_existence": "PASS -- every symbol referenced by a `from format_factory.safetensors[.adapters] import ...` statement in the README (8 distinct names across 4 code blocks) actually exists on the current module, checked via hasattr, not merely assumed from the source text",
    "example_execution": "PASS, all 4 README code blocks executed end-to-end against real corpus samples: (1) read_header + safe_open + tensor_region + release, against samples/by-format/safetensors/valid/multi-tensor.safetensors -- correctly reports mode and full_decode_required=False as documented; (2) load + dump (FF6-EVENT-000340, new this tick), against the same real corpus sample -- load() returns a SafeTensorsDocument exposing document.tensors (a mapping of both real tensor names, 'alpha' and 'beta') and document.tensor_bytes(name); dump() to a fresh path succeeds and the reloaded copy's own tensor names and raw bytes match the original byte-for-byte, proving the documented round-trip claim directly rather than assuming it from the source text; (3) load_index + resolve_shard, against a real temp index file -- resolves the shard path correctly; (4) to_numpy (borrowed and copy=True) + to_torch, against the same real sample -- borrowed view is read-only (WRITEABLE=False) as documented, copy view is writable, to_torch correctly raises ModuleNotFoundError since torch is not installed in this environment (proving the adapter's own lazy-import boundary, not a hard dependency)",
    "changelog_content_accuracy": "PASS (FF6-EVENT-000350, same check dimension opened for ubl/nrrd/ora/xliff at Events 346-349) -- CHANGELOG.md's existing single generic bullet ('strict structural validation, sub-byte dtype support, deterministic writing, lazy memory mapping, and optional NumPy/PyTorch adapters') gestured at some capability but named no concrete symbols and omitted the full in-memory load/dump API (this session's own Event 340 finding), the sharded index lifecycle, and validate()'s own diagnostic codes entirely. Appended 4 new bullets to the existing '## 0.2.0.dev0' section (pyproject.toml version unchanged, confirming no new heading was needed); every top-level symbol cited (12 total, re-extracted directly from __all__ rather than assumed -- caught and corrected an initial draft that incorrectly implied `resolve_shard` was a top-level function when it is actually a method on `SafeTensorsShardIndex`) verified via hasattr against the current module before writing."
  },
  "incidental_finding": "FF6-EVENT-000340: found and closed a genuine documentation-completeness gap during a fresh capability survey of this format (0 unresolved obligations, prompting a check of Event 327's own still-open narrative invariant \"every declared developer capability meets the same production standard as the parser and writer\"): load/loads/dump/dumps are exported in __all__ and fully functional, but were entirely absent from the README before this tick -- a developer reading the prior README would have no way to discover the full in-memory load/write API exists at all, only the lazy read_header/safe_open path. This gate's own prior PASS verdict was accurate for what it checked (every symbol the README DID reference existed and worked), but did not itself catch symbols that exist and are exported yet are never mentioned in the README at all -- a scope boundary of the existing check worth noting for future gate-tooling improvement, not a defect in the prior PASS. While executing the adapters example (unchanged from the prior tick), an initial test mistake (not releasing a borrowed to_numpy() view before the safe_open context exited) correctly triggered the documented SafeTensorsError 'cannot close the SafeTensors payload while borrowed views are active' -- the README's own documented safety guarantee firing exactly as described, not a defect. Confirms the borrowed-view discipline the docs describe is genuinely enforced, not just asserted in prose. FF6-EVENT-000350: closed the CHANGELOG staleness gap (opened for ubl at Event 346) for safetensors -- ipynb remains the last format open.",
  "note": "This is one of product-goal.yaml's own 10 completion invariants (installed-wheel, independent-oracle, corpus, security, resource, typing, documentation, compatibility, performance, reproducible-build) -- this file records the documentation invariant only, not a certification claim for the format as a whole.",
  "commit": "pending"
}
