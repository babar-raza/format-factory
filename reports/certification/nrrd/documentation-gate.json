{
  "format_id": "nrrd",
  "gate": "documentation",
  "status": "PASS",
  "verdict": "PASS",
  "docs_present": ["README.md", "CHANGELOG.md", "MIGRATION.md", "SECURITY.md"],
  "readme_line_count": 164,
  "readme_sections": [
    "Installation", "Common lifecycle", "Attachment-form conversion", "Encoding and dtype conversion",
    "Header-only inspection", "Lazy payload access", "Space transforms", "Preservation and array access",
    "Security and resource limits", "Current scope", "License"
  ],
  "checks_performed_this_tick": {
    "readme_substantially_rewritten": "The prior README was 37 lines with no section headers and documented only the basic lifecycle -- it did not mention attachment-form conversion (convert_to_attached/convert_to_detached_list, this session's own Event 316 work), encoding/dtype conversion, lazy payload access, or space transforms, all of which are real, existing, tested public API. Rewritten to 144 lines / 10 sections covering the real feature depth, following the exact same pattern already established for xliff/ora.",
    "symbol_existence": "PASS -- every symbol referenced by a `from format_factory.nrrd import ...` statement in the README (19 distinct names across 8 code blocks, +read_header this tick) actually exists on the current module, checked via hasattr, not merely assumed from the source text",
    "example_execution": "PASS -- the README's own 'Common lifecycle' example (probe/load/validate/dump) was executed verbatim against a real corpus sample (samples/by-format/nrrd/valid/2d-float32.nrrd) end-to-end: probed true, loaded, validated clean, and dumped to a real output file. The new 'Header-only inspection' example (FF6-EVENT-000343) was executed against two real corpus samples with contrasting encodings: samples/by-format/nrrd/valid/2d-float32.nrrd (raw encoding) reports access.mode='memory_mapped'/zero_copy=True, while samples/by-format/nrrd/valid/gzip-encoded.nrrd reports access.mode='streaming_decode'/zero_copy=False -- confirming the documented eligibility distinction against two real, differently-encoded files rather than assuming it from the source text."
  },
  "incidental_finding": "FF6-EVENT-000343: applying the same __all__-vs-README diff technique used for safetensors/ipynb/xliff (Events 340-342) to nrrd (58 exported symbols vs. ~20 previously documented/named) surfaced read_header() as the most consequential undocumented capability: a real, standalone, header-only parse entry point (never touches payload bytes) that open_lazy_payload() itself calls internally, directly paralleling safetensors' own already-documented read_header/safe_open relationship -- but nrrd's README only documented the combined open_lazy_payload() path, never the standalone header-only one. Closed that gap this tick, placed directly before 'Lazy payload access' since it is the entry point that section's own function builds on. Other candidates surveyed but deliberately left for a future tick: convert_endian (a third conversion capability alongside the already-documented convert_dtype/convert_encoding), RoundingPolicy (parallel to the documented OverflowPolicy), axis_order_report/AxisOrderReport and flatten_nrrd_array (added this same session at Events 328-331, never README-documented at the time they were added), reshape_nrrd_array, dump_detached, decode_nrrd_data/encode_nrrd_data (low-level codec functions), and the load_nrrd/probe_nrrd/write_nrrd alternate entry points -- named here so a future tick does not re-run the same __all__ extraction from scratch.",
  "honest_gap": "Not every one of the README's 8 code examples was executed end-to-end this tick (the attachment-conversion, dtype-conversion, and space-transform examples reference illustrative variables/files a real caller would supply) -- symbol-existence was verified for all of them via direct API/signature inspection, matching the exact scope already established for xliff/ora's own documentation gates.",
  "note": "This is one of product-goal.yaml's own 10 completion invariants (installed-wheel, independent-oracle, corpus, security, resource, typing, documentation, compatibility, performance, reproducible-build) -- this file records the documentation invariant only, not a certification claim for the format as a whole.",
  "regression": {
    "nrrd_suite": "675 passed, 1 skipped (unchanged by the README rewrite)"
  },
  "commit": "pending"
}
