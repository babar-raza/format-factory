diff --git a/src/python/abw/__init__.py b/src/python/abw/__init__.py
index 2937f20..a290466 100644
--- a/src/python/abw/__init__.py
+++ b/src/python/abw/__init__.py
@@ -21,6 +21,34 @@ from .abw_codec import (
     export_to_txt,
     export_to_html,
     get_metadata,
+    export_to_json,
+    edit_paragraph,
+    export_to_csv,
+    get_paragraph,
+    search_paragraph,
+    get_word_count,
+    search_replace_paragraph,
+    merge_abw,
+    word_frequency,
+    truncate_paragraphs,
+    get_unique_words,
+    append_paragraph,
+    split_paragraphs,
+    get_char_count,
+    join_paragraphs,
+    replace_in_paragraphs,
+    export_to_markdown,
+    get_paragraph_at,
+    paragraph_lengths,
+    reverse_paragraphs,
+    word_wrap,
+    has_paragraph,
+    first_paragraph,
+    last_paragraph,
+    count_words,
+    paragraph_at,
+    text_stats,
+    export_to_plain_text,
 )
 
 __all__ = [
@@ -36,6 +64,34 @@ __all__ = [
     "export_to_txt",
     "export_to_html",
     "get_metadata",
+    "export_to_json",
+    "edit_paragraph",
+    "export_to_csv",
+    "get_paragraph",
+    "search_paragraph",
+    "get_word_count",
+    "search_replace_paragraph",
+    "merge_abw",
+    "word_frequency",
+    "truncate_paragraphs",
+    "get_unique_words",
+    "append_paragraph",
+    "split_paragraphs",
+    "get_char_count",
+    "join_paragraphs",
+    "replace_in_paragraphs",
+    "export_to_markdown",
+    "get_paragraph_at",
+    "paragraph_lengths",
+    "reverse_paragraphs",
+    "word_wrap",
+    "has_paragraph",
+    "first_paragraph",
+    "last_paragraph",
+    "count_words",
+    "paragraph_at",
+    "text_stats",
+    "export_to_plain_text",
 ]
 
 __version__ = "0.1.0.dev0"
