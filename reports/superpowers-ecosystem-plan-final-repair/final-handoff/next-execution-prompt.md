# Next Execution Prompt
## Sprint: FORMAT-FACTORY-SUPERPOWERS-AGENTIC-AUTONOMY-EXECUTION-001
Generated by: FORMAT-FACTORY-SUPERPOWERS-ECOSYSTEM-PLAN-FINAL-REPAIR-001
Date: 2026-06-06

---

## MANDATORY FIRST STEPS (do before any lane work)

1. Read `reports/supervisor/session-resume.md`
2. Read `reports/supervisor/approval-gates.md`
3. Run canonical command discovery:
   ```bash
   python tools/supervisor/supervisor_loop.py --help 2>&1 | grep autonomous-cycle
   python tools/supervisor/build_declaration_review_package.py --help 2>&1 | grep declaration
   ```
4. Read `reports/superpowers-ecosystem-plan-final-repair/runtime-verification/tool-status-runtime.json` — do NOT re-verify what is already documented there
5. Read `reports/superpowers-ecosystem-plan-final-repair/final-handoff/execution-readiness-checklist.json`

---

## Sprint Purpose

Build the Superpowers-first agentic autonomy architecture:
- next_action_schema.py + next_action_runner.py (H3 minimum: runner executes one action)
- backend_selector.py (priority chain: SUPERPOWERS → SESSION_SKILL_TOOL → SUBAGENT → MCP → LLM → DETERMINISTIC → CLI)
- backends/ directory (MCP, LLM API, local deterministic)
- H3 proof: runner dispatches one action via next_action_runner.py
- H4 proof (if achievable): two sequential runner cycles
- Evidence bundle: autonomous-cycle + ZIP + SHA-256

---

## Tool Authority Boundaries (CRITICAL — read before any tool use)

| Tool | Role | NOT Authority For |
|------|------|-------------------|
| Superpowers plugin | Skill execution | Supervisor grading |
| SESSION_SKILL_TOOL (.claude/commands/) | Skill execution in session | External host continuation |
| Task Master | Task queue (advisory) | Taskcard CLOSED state |
| Cognee | Memory/RAG lookup | Proof generation |
| Skill Seekers | Skill generation (candidate only) | Installed skill proof |
| Format Factory Supervisor | SOLE FINAL AUTHORITY | — |

**TM "done" ≠ taskcard CLOSED.** Taskcard CLOSED requires evidence check by coordinator.
**Cognee recall ≠ proof.** Memory items must be re-verified against current file state.
**Generated SKILL.md ≠ installed skill.** Skill must be callable via Skill tool with execution evidence.

---

## MCP Status Layers (do NOT assume L5=READY from config alone)

| Layer | Description | Current Status |
|-------|-------------|----------------|
| L1 | Config present (.vscode/mcp.json) | PRESENT |
| L2 | Daemon running | UNKNOWN (probe at runtime) |
| L3 | Tool callable | UNKNOWN |
| L4 | API key configured | TASK_MASTER_API_KEY ABSENT |
| L5 | READY (all above true) | false |

If Task Master needed: prompt user to set TASK_MASTER_API_KEY and start MCP daemon.

---

## Proof Level Requirements

| Level | Description | Minimum Required |
|-------|-------------|-----------------|
| H1 | next-action.json generated | INSUFFICIENT |
| H2 | next-action.json generated + validated | INSUFFICIENT |
| H3 | runner dispatches one action via next_action_runner.py | MINIMUM REQUIRED |
| H4 | Two sequential runner cycles | STRETCH GOAL |
| H5 | Agentic backend (SESSION_SKILL_TOOL or CLAUDE_AGENT_SUBAGENT) via runner | STRETCH GOAL |
| H6 | External host continuation (CLAUDECODE=0 only) | NOT IN THIS SESSION |

**Two-cycle proof MUST go through next_action_runner.py dispatch.**
**NOT acceptable**: fabricated session narrative, Agent tool call without runner dispatch, Claude CLI nested session.

---

## Lanes and Taskcard Ownership

### L0: Preflight (Coordinator)
**Owned**: `reports/superpowers-agentic-autonomy/`
**Creates**: 00-preflight.md, lane-claims.json, execution-state.json, taskcard-state.json, touched-files-ledger.jsonl
**Shared file rule**: execution-state.json and taskcard-state.json are COORDINATOR WRITE ONLY
**Forbidden (modification)**: src/, tests/, registry/, AGENTS.md, GOVERNANCE.md

### L1: Superpowers Workflow Audit
**Owned**: `reports/superpowers-agentic-autonomy/audit/`, `superpowers-workflow-model.md`
**Creates**: superpowers-workflow-model.md (integration design), audit/tool-boundary-analysis.json
**Action**: Read docs/governance/superpowers-skill-intake.md; document Superpowers integration design
**Proof required**: superpowers-workflow-model.md non-empty
**Forbidden**: src/, tools/supervisor/ (modification), tests/, registry/

### L2: Capability Taxonomy Update
**Owned**: `reports/superpowers-agentic-autonomy/capability-taxonomy-update.json`
**Creates**: Updated taxonomy from TC-FR-008 revised-capability-taxonomy.json
**Proof required**: JSON valid, SESSION_SKILL_TOOL distinct entry, SUPERPOWERS_LOCAL_PLUGIN_NOT_FOUND status confirmed

### L3: Schema + Runner (14 validators before implementation)
**Owned**: `tools/supervisor/next_action_schema.py`, `tools/supervisor/next_action_runner.py`, `tools/supervisor/execution_backend.py`
**VALIDATOR-FIRST TDD GATE**: Write 14 validators/tests that cover:
  1. next_action schema validates correctly (required fields)
  2. schema rejects missing action_type
  3. schema rejects missing backend
  4. runner imports without error
  5. runner rejects invalid next-action.json
  6. runner dispatches to LOCAL_DETERMINISTIC backend
  7. runner writes execution transcript to declared path
  8. runner validates proof_path is non-empty
  9. execution_backend base class has required interface methods
  10. runner handles backend unavailable (fallback triggered)
  11. runner does NOT allow host to create proof file
  12. runner logs INTRA_SESSION_DELEGATION_PROOF for Agent-based dispatch
  13. runner handles exit code mapping (0=success, 3=rework, 9=error)
  14. runner rejects next-action.json with proof_path already existing (anti-fabrication)

Then implement next_action_schema.py, execution_backend.py, next_action_runner.py to pass all 14 validators.
**Evidence**: tests pass, transcript at `reports/superpowers-agentic-autonomy/runner-proof/h3-transcript.json`

### L4: Backend Selector
**Owned**: `tools/supervisor/backend_selector.py`
**Priority chain**: SUPERPOWERS_LOCAL_PLUGIN → SESSION_SKILL_TOOL → CLAUDE_AGENT_SUBAGENT → REPO_LOCAL_SKILL → MCP_SUPERPOWERS → LLM_API → LOCAL_DETERMINISTIC → CLAUDE_CLI_OPTIONAL → MANUAL_EXTERNAL_GATE
**Selection logic**: read tool-status-runtime.json for current availability; skip unavailable backends; log selection reason
**Evidence**: import works, unit test for selector passes

### L5: MCP Superpowers Backend
**Owned**: `tools/supervisor/backends/mcp_superpowers_backend.py`
**Note**: This sprint = STUB only if MCP L5 not READY. Stub must return BLOCKED_MCP_NOT_READY with instructions.
**Evidence**: file exists, imports without error

### L6: LLM API Backend
**Owned**: `tools/supervisor/backends/llm_api_backend.py`, `tools/supervisor/llm_backend_config.py`
**Reads**: tools/llm/endpoints.yaml for endpoint config (do NOT hardcode URLs)
**Available credentials**: PROFESSIONALIZE_API_KEY PRESENT (see tool-status-runtime.json)
**Evidence**: file exists, endpoints.yaml read confirmed, unit test passes

### L7: Local Deterministic Backend
**Owned**: `tools/supervisor/backends/local_deterministic_backend.py`
**H3 proof dependency**: This backend MUST work for H3 proof (runner executes one action locally)
**Evidence**: H3 proof transcript at `reports/superpowers-agentic-autonomy/runner-proof/h3-transcript.json` with proof_path written by runner (NOT by host)

### L8: Host Daemon (Scaffold Only)
**Owned**: `tools/supervisor/autonomous_host_daemon.py`, `scripts/format_factory_autonomous_host.sh`
**Note**: CLAUDECODE=1 blocks invocation from this session. Scaffold only; external host invocation = FUTURE.
**Evidence**: file exists, imports without error, --help works

### L9: Two-Cycle Proof
**Owned**: `reports/superpowers-agentic-autonomy/two-cycle-proof/`
**Minimum**: H3 proof (one cycle). H4 (two cycles) is stretch goal.
**CRITICAL**: proof must go through next_action_runner.py, NOT be fabricated as text output
**Evidence**: h3-transcript.json exists with runner exit code + proof_path content

### L10: Superpowers Skills Proof
**Owned**: `reports/superpowers-agentic-autonomy/superpowers-proof/`
**Documents**: SESSION_SKILL_TOOL availability (.claude/commands/ verified); attempt to use Skill tool in runner; classify result
**Evidence**: skill-proof-report.json with availability_status and invocation attempt result

### L11: Tests
**Owned**: `tests/supervisor/test_execution_backends.py`, `tests/supervisor/test_backend_selector.py`, `tests/supervisor/test_two_cycle_proof.py`
**Minimum**: 14 validators from L3 passing; backend selector test passing
**Evidence**: pytest output, test count

### L12: Independent Verification
**Owned**: `reports/superpowers-agentic-autonomy/iv/`
**Creates**: iv-report.json (12 questions, verdict), iv-anti-false-autonomy-check.json
**14 anti-false-autonomy validators to check**:
  1. next-action.json was dispatched via runner (not just generated)
  2. proof_path was created by runner/backend (not by host)
  3. TM "done" items all have evidence check
  4. Cognee recalls all re-verified against current file state
  5. No generated SKILL.md counted as installed skill
  6. SESSION_SKILL_TOOL distinct from Superpowers plugin
  7. H3 proof uses next_action_runner.py (not fabricated)
  8. Backend selector reads tool-status-runtime.json (not hardcoded)
  9. MCP status not assumed from config alone
  10. LLM endpoint config read from endpoints.yaml (not hardcoded)
  11. Package-107 pattern absent (host did NOT create proof file)
  12. INTRA_SESSION_DELEGATION_PROOF correctly classified
  13. Evidence bundle absolute path reported to user
  14. manifest_missing_count == 0

### LE: Evidence
**Owned**: `.local/evidences/superpowers-agentic-autonomy/`
**Creates**: evidence-declaration.yaml, evidence-manifest.yaml
**Runs**: autonomous-cycle + build_declaration_review_package
**Reports**: absolute path + SHA-256

---

## OpenSpec Pilot Change Template (Input Only)

OpenSpec is NOT yet installed (NOT_FOUND). Use as design input only.
When installed (future sprint), OpenSpec change would be:
```yaml
change_id: SPEC-AGENTIC-AUTONOMY-001
target: tools/supervisor/next_action_runner.py
description: Add next_action_runner.py to supervisor pipeline
spec_layer: execution
```
OpenSpec = spec/change layer ONLY. NOT an executor. NOT Format Factory authority.

---

## Task Master Status Check (Runtime L1–L5)

At sprint start, classify Task Master:
```python
import json, pathlib
mcp = json.load(open('.vscode/mcp.json'))
servers = list(mcp.get('servers', mcp.get('mcpServers', {})).keys())
print('L1 config:', 'task-master-ai' in servers)
# L2-L5 require daemon probe — classify as UNKNOWN if not provable
```

Report TM status as one of: L5_READY / L4_API_KEY_MISSING / L3_TOOL_NOT_CALLABLE / L2_DAEMON_NOT_RUNNING / L1_CONFIG_ONLY

---

## Ecosystem Tool Status Summary (from TC-FR-001 verification)

| Tool | Status | Notes |
|------|--------|-------|
| supervisor_loop.py autonomous-cycle | VERIFIED | Canonical command |
| build_declaration_review_package.py | VERIFIED | --declaration arg present |
| .claude/commands/ | VERIFIED (24 files) | SESSION_SKILL_TOOL available |
| .vscode/mcp.json task-master-ai | L1_CONFIG_PRESENT | Daemon status UNKNOWN |
| cognee | NOT_FOUND | pip install cognee when needed |
| openspec | NOT_FOUND | npm install -g openspec when needed |
| skill_seekers | NOT_FOUND | pip install when needed |
| ANTHROPIC_API_KEY | ABSENT | User must set |
| PROFESSIONALIZE_API_KEY | PRESENT | usable for LLM API backend |
| TASK_MASTER_API_KEY | ABSENT | User must set for TM L5 |
| Local Ollama | UNKNOWN | Probe localhost:11434 at runtime |
| Local LM Studio | UNKNOWN | Probe localhost:1234 at runtime |

---

## Evidence Bundle Requirements

```bash
# Sprint closeout — MANDATORY
python tools/supervisor/supervisor_loop.py autonomous-cycle \
  --declaration .local/evidences/superpowers-agentic-autonomy/evidence-declaration.yaml

python tools/supervisor/build_declaration_review_package.py \
  --declaration .local/evidences/superpowers-agentic-autonomy/evidence-declaration.yaml

# SHA-256
python -c "
import hashlib, pathlib
p = list(pathlib.Path('.local/supervisor/reviews/superpowers-agentic-autonomy').glob('*.zip'))[0]
print('ABSOLUTE PATH:', p.resolve())
print('SHA-256:', hashlib.sha256(p.read_bytes()).hexdigest())
"

# Manifest-vs-ZIP validation (manifest_missing_count MUST be 0)
python -c "
import zipfile, yaml, pathlib
decl = yaml.safe_load(open('.local/evidences/superpowers-agentic-autonomy/evidence-declaration.yaml'))
declared = {item['path'] for item in decl.get('artifacts', [])}
zf = zipfile.ZipFile(list(pathlib.Path('.local/supervisor/reviews/superpowers-agentic-autonomy').glob('*.zip'))[0])
in_zip = set(zf.namelist())
missing = declared - in_zip
print(f'manifest_missing_count={len(missing)}')
assert len(missing) == 0
"
```

Report to user: **absolute path** + SHA-256 before declaring sprint complete.

---

## Hard Rules (NEVER autonomous)

- NO git push, NO git commit
- NO Gate 8 or Gate 11 approval
- NO package publication
- NO MCP activation changes
- NO destructive git (no reset --hard, stash, clean)
- NO credential logging
- NO external tool treated as Format Factory authority
- NO verdict preselection — evidence-derived only
- NO fabricated proof (host must NOT create proof files)
- NO nested Claude CLI invocation from inside Claude Code session (CLAUDECODE=1)

---

## Verdict Mapping

| Condition | Verdict |
|-----------|---------|
| H3 proof via runner, all 14 validators pass, IV PLAN_READY, ZIP exists | COMPLETE_RUNNER_H3_PROVEN |
| H4 (two cycles) additionally proven | COMPLETE_RUNNER_H4_PROVEN |
| H5 (agentic backend via runner) | COMPLETE_BACKEND_SELECTOR_AGENTIC_PROVEN |
| 14 validators pass, runner works, but no two-cycle proof | PARTIAL_BACKEND_SELECTOR_SINGLE_CYCLE |
| Only LOCAL_DETERMINISTIC backend operational | PARTIAL_EXTERNAL_HOST_REQUIRED |
| ZIP failed to build | BLOCKED_EVIDENCE_PACKAGE_FAILED |

---

## Execution Order

1. L0 Preflight (required first)
2. L1–L2 Audit (parallel, read-only)
3. L3 Schema+Runner (14 validators FIRST, then implement)
4. L4 Backend Selector (after L3 execution_backend.py exists)
5. L5, L6, L7 Backends (parallel — distinct files)
6. L8 Host Daemon (parallel with L5–L7)
7. L9 Two-cycle proof (after L3+L7 complete)
8. L10 Superpowers proof (parallel with L9)
9. L11 Tests (after L3–L8 complete)
10. L12 IV (after L11 tests pass)
11. LE Evidence (last — after all lanes complete)
