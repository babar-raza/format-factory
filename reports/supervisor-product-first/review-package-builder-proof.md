usage: build_declaration_review_package.py [-h] --declaration DECLARATION
                                           [--repo-root REPO_ROOT]
                                           [--out-dir OUT_DIR]

Build a declaration review package ZIP

options:
  -h, --help            show this help message and exit
  --declaration DECLARATION
                        Path to evidence-declaration.yaml
  --repo-root REPO_ROOT
  --out-dir OUT_DIR     Output directory (default:
                        .local/supervisor/reviews/<run_id>/)
