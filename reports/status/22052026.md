## Layman status

We are **no longer lost in drift**. The project is back on a controlled track, but it is **not fully finished yet**.

Think of Format Factory as a **machine/factory** that should take a file format, study it, collect proof, build parsers/writers, test them, and finally produce Python and .NET products. Right now, the factory exists and many parts work. But the final packaging/inspection station still has a few mistakes, so I would not yet call the factory production-complete.

The latest real status after R47 is:

```text
Good progress: yes
Back under governance: mostly yes
Two-product local RC: close, but not cleanly closed
Ready for public release: no
Ready to continue scaling to more formats: yes, with controlled fixes
```

## What the system does well now

The system is good at **not trusting claims blindly**. That is important. It now has registries, taskcards, evidence bundles, phase audits, tests, validators, package manifests, and state snapshots. This means an agent cannot simply say “done” without leaving evidence.

It is also good at moving a format through a repeatable pipeline. For FODS and FODT, the system can already:

1. track the format in a registry,
2. connect it to requirements,
3. run parser/writer tests,
4. build Python wheels and source packages,
5. build .NET NuGet packages,
6. bundle proof artifacts,
7. run evidence checks,
8. keep AI as helper only, not authority.

R47 was a major step because it finally included real package files in the evidence bundle: Python wheels, Python source packages, and .NET `.nupkg` files. Before that, reports claimed packages existed, but the uploaded bundle did not actually contain them.

## What the system can repeat again and again

The repeatable parts are now fairly strong:

| Area                       | Repeatability                                                              |
| -------------------------- | -------------------------------------------------------------------------- |
| Registry/status tracking   | Good                                                                       |
| Evidence bundle creation   | Good, but closeout order still needs fixing                                |
| Python tests for FODS/FODT | Strong                                                                     |
| Python package creation    | Improving; now real artifacts exist                                        |
| .NET package creation      | Improving; artifacts exist, but consumer replay still needs stronger proof |
| AI no-live checks          | Good                                                                       |
| Spec/phase audit process   | Started and becoming repeatable                                            |
| Validator checks           | Much stronger than before, but still needs final closeout hardening        |

The main repeatability weakness is not the core code now. It is **agent closeout discipline**. The agent keeps doing real work, then forgets to refresh the final verdict/proof after bundle validation. That creates contradictions like:

```text
Bundle is actually built
but final-verdict.md still says BUNDLE_VALIDATION: PENDING
```

That is not a product-code problem. It is a process-closeout problem, and it must be fixed with a hard closeout script/skill.

## How the system acquires and processes a format

In simple terms, the system should work like this:

### 1. Find the official source

For a format, the system first asks: where is the real truth?

For example:

* official specification,
* RFC,
* ODF standard,
* vendor documentation,
* known sample corpus,
* format reference files.

The system should not trust random blog posts or AI guesses as authority.

### 2. Cache or record the source

If the spec can be cached locally, it goes into a local spec cache. If it cannot be committed for licensing reasons, the system records metadata instead:

* source URL,
* version,
* hash,
* date acquired,
* legal/redistribution status,
* whether local cache exists.

This is important because future agents should not “rediscover” the format every time.

### 3. Collect samples

The system needs real files to test against. These samples must also have provenance:

* where did the sample come from?
* is it synthetic or real?
* can it be redistributed?
* what is its hash?
* which tests use it?

This is the phase we are now auditing more deeply.

### 4. Convert specs and samples into requirements

The system turns the spec/sample knowledge into concrete requirements:

* what the parser must read,
* what fields matter,
* what errors must be handled,
* what security cases matter,
* what features are in scope or out of scope.

AI can help summarize, but the final accepted requirements must be validated by tests and evidence.

### 5. Build parser/prototype

The parser reads the format and turns it into a neutral internal model. The neutral model is important because it lets Python and .NET share the same conceptual behavior.

For example, a spreadsheet-like format might become:

```text
Workbook
  Sheets
    Rows
      Cells
        value
        value_type
        formula
```

### 6. Test parser and security behavior

The system checks:

* valid files parse,
* bad files fail safely,
* malformed files do not crash,
* dangerous paths are not extracted,
* compressed formats have limits,
* fuzz tests do not break parser assumptions.

### 7. Build writer/exporter where allowed

For some formats, reading is enough at first. For stronger product readiness, the system adds writing/exporting:

* save back to same format,
* export to CSV/TXT/HTML/JSON,
* preserve typed values,
* preserve formulas,
* preserve paragraphs/headings/tables, etc.

### 8. Build Python and .NET products

The system then packages the implementation:

* Python wheel,
* Python source distribution,
* .NET NuGet package.

This is where we are now with FODS/FODT. The packages exist, but the final validation/consumer proof needs one more strong cleanup.

### 9. Prove packages from outside the source tree

This is critical. A package is not really proven if it only works inside the repo.

The final proof should be:

* install Python wheel in a clean environment,
* run smoke tests from the installed package,
* restore NuGet package in a clean consumer project,
* run real consumer code,
* prove artifacts in the bundle match hashes.

R47 got closer, but this still needs clean closeout.

## Is it generic?

**Yes, the design is generic.**

The system is not hardcoded only for FODS/FODT. It already has many formats in the registry and sample folders. The workflow can be reused for future formats.

But “generic” does not mean “every future format is automatic.” File formats differ a lot.

A simple format like PPM, PBM, PGM, CSV, TSV, or ZST can move faster.

A complex format like XCF, ODS, ODT, FODP, FODG, or Gnumeric needs more careful handling because it may involve:

* complicated internal structure,
* compression,
* XML,
* binary chunks,
* embedded resources,
* security risks,
* licensing/sample issues,
* many optional features.

So the honest answer is:

```text
The pipeline is generic.
The effort per format will vary.
Simple formats should be easy.
Complex formats still need controlled phase-by-phase work.
```

## How strong is the system?

I would rate it like this:

| Part                            | Strength                                                   |
| ------------------------------- | ---------------------------------------------------------- |
| Governance model                | Strong                                                     |
| Evidence-first culture          | Strong                                                     |
| Registry/taskcard approach      | Strong                                                     |
| Python FODS/FODT implementation | Moderate to strong                                         |
| .NET FODS/FODT implementation   | Moderate, still needs consumer proof and deeper capability |
| Package artifact proof          | Recently improved, not fully closed                        |
| Phase audit process             | Newly started, good direction                              |
| Agent closeout discipline       | Still weak                                                 |
| Future-format scalability       | Good foundation, needs validators/skills hardened          |
| Product/publication readiness   | Not yet                                                    |

The biggest weakness is not that the system cannot build things. It can. The weakness is that the agent repeatedly **overclaims completion before the last verification step is truly clean**.

That is why I keep pushing the prompts to require:

* actual artifacts,
* extracted bundle replay,
* no stale `PENDING`,
* no placeholder proof,
* phase-by-phase audits,
* final adversarial review.

## Can it handle more future formats with few changes?

For many future formats, yes.

The system should be able to add a new format by adding or updating:

1. registry entry,
2. spec/provenance record,
3. sample provenance,
4. generated requirements,
5. parser/prototype,
6. tests,
7. package mapping,
8. evidence contract.

For simple formats, this could become a mostly repeatable process.

For complex formats, it will still need format-specific work, but the **control framework** will stay the same. That is the important win. We are building a factory, not hand-building every product from scratch.

## Can we test it phase by phase?

Yes. This is exactly the right direction now.

Instead of asking “is the whole system good?”, we should test each phase separately. That makes problems easier to find.

The phase sequence should be:

| Phase   | Plain-English meaning                                              | Current strength                                                          |
| ------- | ------------------------------------------------------------------ | ------------------------------------------------------------------------- |
| Phase 1 | Did we get the right official spec/source?                         | Started; core formats decent; minor formats partial                       |
| Phase 2 | Do we have valid samples and know where they came from?            | Started in R47; incomplete because not all sample folders were covered    |
| Phase 3 | Did we turn specs/samples into parser requirements and prototypes? | Exists for FODS/FODT; needs formal audit next                             |
| Phase 4 | Do parser, neutral model, fuzz/security/oracle tests work?         | Partially strong for some formats; should be audited after Phase 3        |
| Phase 5 | Is the format authorized and mapped to product behavior?           | Exists but must be checked carefully before product claims                |
| Phase 6 | Can we build real packages and prove them from clean installs?     | Almost there for FODS/FODT; R47 fixed artifacts, closeout still defective |
| Phase 7 | Can we approve commercial/publication readiness?                   | Not yet; Gate 11 remains blocked, correctly                               |

This phase approach is the right way to prevent drift. It makes the system prove each layer before trusting the next layer.

## Where exactly are FODS and FODT?

They are the leading two products.

### FODS

Status: **close to local RC**, but not fully closed.

Good:

* parser tests pass,
* writer work exists,
* package artifacts now exist,
* Python wheel installs,
* .NET package exists.

Still needs:

* fix typed value schema issue: `type` vs `value_type`,
* prove typed values round-trip correctly,
* finish clean closeout,
* prove .NET consumer replay where SDK is available.

### FODT

Status: **close to local RC**, but still alpha-scope.

Good:

* parser tests pass,
* writer tests exist,
* package artifacts now exist,
* Python wheel installs,
* .NET package exists.

Still needs:

* keep writer scope honest,
* improve semantic proof where possible,
* prove .NET consumer replay,
* avoid overclaiming headings/lists/tables if not fully supported.

## My plain-English conclusion

We have built something valuable and fairly serious.

This is no longer just scattered code. It is becoming a repeatable system with:

* governance,
* evidence,
* registry,
* phases,
* artifacts,
* tests,
* package proof,
* AI acceleration under control.

But it is **not yet a polished product machine**. It is more like a factory whose main production line works, but the final quality-control station still needs tightening.

The biggest remaining fix is:

```text
Stop allowing the agent to claim “complete” until the final bundle,
final verdict, actual artifacts, replay proof, and state snapshot all agree.
```

Once that closeout discipline is fixed, the system should be strong enough to scale to more formats in a controlled way.


---

Yes — your clarification changes the target from **“parser/package POC”** to **“editable object-model product POC.”** That is the right correction.

The immediate POC should not be accepted merely because a library can parse a file and produce a package. For your goal, each product must prove:

```text
Load → object model → edit meaningful item → save same format → reload → verify edit + preservation
```

Export can come slightly later, but the design must already point toward export.

## 1. What the final Python/.NET products must do

Your product requirement should be stated like this:

| Requirement      | Meaning                                                                         |
| ---------------- | ------------------------------------------------------------------------------- |
| Parse/load       | Open a real file and build an in-memory model.                                  |
| Edit             | Change a meaningful object based on document type.                              |
| Save same format | Write back to the original format.                                              |
| Preserve         | Keep untouched content as close to original as possible.                        |
| Export           | Save to other formats later, preferably using our own Format Factory libraries. |

So for a spreadsheet-like format such as FODS:

```text
Load workbook
→ access sheet/cell/style/formula
→ edit cell value/style/formula
→ save as FODS
→ reload saved FODS
→ verify changed cell
→ verify unchanged cells/styles/formulas survived
```

For a document-like format such as FODT:

```text
Load document
→ access paragraphs/headings/runs/styles/alignment/font info
→ edit heading text/font/size/alignment
→ save as FODT
→ reload saved FODT
→ verify edit
→ verify other paragraphs/styles/tables survived
```

That is stronger than what the current system has mostly proven so far.

## 2. Object model is the right direction

I agree with your instinct: **object model first** is the correct architecture.

A parser-only approach is too weak. If we only parse into loose dictionaries, future editing becomes fragile. For production, the system needs a stable in-memory model.

The better architecture is:

```text
Original file
   ↓
Parser
   ↓
Object model / DOM-like model
   ↓
Editing API
   ↓
Writer / serializer
   ↓
Saved same-format file
   ↓
Reload verification
```

For spreadsheets, the model should look roughly like:

```text
Workbook
  Sheets
    Rows
      Cells
        address/name
        value
        value_type
        formula
        style
          font
          fill
          border
          number_format
          alignment
```

For documents:

```text
Document
  Sections
    Blocks
      Paragraph
      Heading
      Table
      List
    Runs
      text
      style
      font
      size
      bold/italic
      alignment
```

This object model gives you future agility. New features become additions to the model and writer, not rewrites of the whole parser.

## 3. Same-format save is the hardest requirement

This is the big one.

“Save same format while preserving everything else” can be done at different quality levels:

| Level   | Meaning                                                                     |
| ------- | --------------------------------------------------------------------------- |
| Level 1 | Parse and regenerate a simplified file.                                     |
| Level 2 | Preserve major structures and content.                                      |
| Level 3 | Preserve styles, metadata, formulas, tables, and layout-sensitive features. |
| Level 4 | Near round-trip fidelity with unknown/unsupported content preserved.        |
| Level 5 | Production-grade compatibility.                                             |

For POC, we should require at least:

```text
Level 2+ for FODS/FODT:
major content preserved,
edited item changes correctly,
known styles/formulas/metadata preserved where claimed,
unsupported features not silently destroyed without warning.
```

For production, we need a preservation strategy.

The best design is probably a hybrid:

```text
Object model for known/editable features
+
raw preservation layer for unknown/unmodified XML/nodes/parts
```

That means:

* known things can be edited through the object model,
* unknown things are carried through untouched where possible,
* the writer does not flatten the whole file unnecessarily,
* preservation is measurable through tests.

This matters especially for ODF-family formats like FODS/FODT.

## 4. Export should be dogfooded through our own libraries

Your export idea is strong.

Instead of each format independently learning how to export to PDF, HTML, CSV, TXT, etc., we should eventually have:

```text
Any supported format
→ object model
→ target format library
→ output file
```

Example:

```text
FODT → DocumentModel → PDF writer
FODS → WorkbookModel → PDF writer or CSV writer
```

That means if we want PDF export, we should acquire/build PDF support in Format Factory, then reuse it.

So future design should split export into two tracks:

### Immediate POC

For FODS/FODT:

```text
load → edit → save same format
```

Maybe also simple exports if already easy:

```text
FODS → CSV / JSON
FODT → TXT / Markdown / HTML
```

### Strategic export train

Start acquiring target export formats:

```text
PDF
HTML
CSV
TXT
Markdown
JSON
possibly SVG/image formats later
```

PDF is important, but it is also complex. The right move is to start a **PDF acquisition train** now, not block FODS/FODT POC on PDF.

## 5. What needs to change in the next prompts

Future prompts should stop treating “package exists” as enough.

They must require this for each POC product:

```text
1. Load real fixture.
2. Build object model.
3. Edit meaningful object.
4. Save same format.
5. Reload saved file.
6. Verify edit.
7. Verify preservation of untouched items.
8. Prove this from installed Python wheel and .NET NuGet consumer project.
```

For FODS, minimum edit proof should include:

```text
cell value
cell type
formula preservation
style or alignment if currently supported
multi-sheet preservation
```

For FODT, minimum edit proof should include:

```text
paragraph text
heading text
heading/style metadata where supported
alignment/font/size if currently supported or taskcarded as missing
table/list preservation if present in sample
```

If a feature is not supported, the system must say:

```text
unsupported in current POC
not silently claimed
taskcard created
```

## 6. How strong is the current system against this clarified target?

Honest answer:

| Area                 | Current strength                              |
| -------------------- | --------------------------------------------- |
| Parse/load           | Good for FODS/FODT                            |
| Basic object model   | Exists, but needs stronger schema discipline  |
| Editing              | Started, but not yet enough                   |
| Same-format save     | Started, but preservation proof is still weak |
| Export               | Not mature yet                                |
| Packaging            | Now much better after R47 artifacts           |
| Replay proof         | Close, but closeout issue remains             |
| Phase audit          | Started correctly                             |
| Production readiness | Not yet                                       |

The major gap is that we have been proving **artifact/package existence**, but now we must prove **editable product behavior**.

That is the right next elevation.

## 7. How AI is currently being used

Based on the evidence bundles and our reviews so far, the current AI usage appears to be mainly:

| AI use                                                   | Status                                                                                                                                |
| -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| Drafting requirements/gaps                               | Yes                                                                                                                                   |
| Assisting phase audits                                   | Yes / planned                                                                                                                         |
| Suggesting tests                                         | Yes / planned                                                                                                                         |
| Summarizing reports                                      | Yes                                                                                                                                   |
| Running as authority                                     | Should be no                                                                                                                          |
| Producing final accepted requirements without validation | Should be forbidden                                                                                                                   |
| Embeddings/vector search                                 | I have not seen strong proof this is operational yet                                                                                  |
| Agent Metrics posting                                    | I have not seen reliable proof from these Format Factory bundles that llm.professionalize usage is being posted to Google Sheet       |
| Local agents/skills/prompts                              | Yes, heavily used                                                                                                                     |
| llm.professionalize.com                                  | Intended/used in the operating model, but every sprint should explicitly prove whether calls happened and whether metrics were posted |

The policy should be:

```text
AI can propose.
AI can accelerate.
AI can compare.
AI can draft tests.
AI can summarize specs.
AI cannot approve.
AI cannot be source of truth.
AI cannot silently change gates.
AI cannot replace evidence.
```

For Agent Metrics, we need to require an explicit AI telemetry lane:

```text
For every sprint:
- Did any LLM call occur?
- Which provider?
- How many calls?
- Token counts if available?
- Was it llm.professionalize.com?
- Was Agent Metrics Google Sheet posting attempted?
- Was it dry-run or production?
- Where is the ledger?
- If no posting occurred, why?
```

Right now, I would not assume Agent Metrics is working for Format Factory unless a bundle proves it with logs/ledger.

## 8. The phase-by-phase view after your clarification

The phase model should now be:

| Phase    | Purpose                           | Product meaning                                     |
| -------- | --------------------------------- | --------------------------------------------------- |
| Phase 1  | Specification ingestion           | Know the official truth.                            |
| Phase 2  | Sample acquisition/provenance     | Have legal, traceable real files.                   |
| Phase 3  | Requirements + prototype          | Convert spec/sample truth into implementation plan. |
| Phase 4  | Object model                      | Build stable editable in-memory representation.     |
| Phase 5  | Parser/load                       | Load real files into object model.                  |
| Phase 6  | Editing API                       | Modify meaningful document-specific items.          |
| Phase 7  | Same-format writer/save           | Save back while preserving untouched content.       |
| Phase 8  | Round-trip/preservation testing   | Reload and prove edit + preservation.               |
| Phase 9  | Package proof                     | Python wheel and .NET NuGet work outside repo.      |
| Phase 10 | Export architecture               | Reuse own libraries for target exports.             |
| Phase 11 | Commercial/publication governance | Approval, docs, release, metrics.                   |

This is slightly better than the earlier phase list because it puts **object model, edit, save, and preservation** explicitly in the pipeline.

## 9. My recommendation

The next sprint prompt should be adjusted around this new target:

```text
R48 should not merely close R47.
R48 should convert FODS/FODT from package RC proof to editable object-model POC proof.
```

The acceptance should be:

```text
FODS Python:
installed wheel can load → edit cell value/type/style or formula → save FODS → reload → verify edit and preservation.

FODT Python:
installed wheel can load → edit paragraph/heading/style where supported → save FODT → reload → verify edit and preservation.

FODS .NET:
NuGet consumer can do same.

FODT .NET:
NuGet consumer can do same.
```

And in parallel:

```text
Start export-format acquisition train, beginning with PDF/HTML/CSV/TXT/Markdown ranking.
```

## 10. Simple bottom line

You are right to raise this now.

We were getting close to proving:

```text
Can package parsers/writers
```

But your real product needs to prove:

```text
Can edit real documents safely through an object model
```

So the next direction should shift from **artifact-contained RC** to **editable object-model POC**.

That is a higher bar, and it is the correct bar for a product that will later grow into production.


---

Yes. The goal should not be “use AI more.” The goal should be:

```text
Use AI to reduce human/agent thinking time,
but force every AI output through deterministic evidence, tests, schemas, and validators.
```

Right now, the agents are weak because they act like general-purpose workers. We should make AI act like a **factory accelerator**: it prepares drafts, compares evidence, proposes gaps, writes tests, reviews code, and coordinates lanes — but the repository tools decide what becomes truth.

## My strongest recommendation

Build an internal **AI Acceleration Layer** inside Format Factory.

Not one-off prompts. A repeatable system.

It should produce typed artifacts like:

```text
ai-drafts/spec-requirements/<format>.yaml
ai-drafts/object-model-gaps/<format>.yaml
ai-drafts/test-proposals/<format>.yaml
ai-drafts/security-risks/<format>.yaml
ai-drafts/preservation-matrix/<format>.yaml
ai-drafts/reviewer-findings/<run>.yaml
```

Then deterministic tools validate or reject them.

AI should be used heavily in these areas:

| Area                       |  AI value | Authority? |
| -------------------------- | --------: | ---------- |
| Spec reading/summarization | Very high | No         |
| Requirements extraction    | Very high | No         |
| Object model design        | Very high | No         |
| Test generation            | Very high | No         |
| Gap detection              | Very high | No         |
| Code review                | Very high | No         |
| Evidence review            | Very high | No         |
| Documentation drafting     |      High | No         |
| Metrics/report summaries   |      High | No         |
| Final gate approval        |      None | Never      |

## Highest-efficiency AI uses

### 1. AI-generated format understanding packs

For every format, AI should produce a draft “format understanding pack.”

Example:

```text
format: FODS
source specs reviewed: ODF 1.3
major structures:
  - workbook
  - table
  - rows
  - cells
  - formulas
  - styles
editable objects:
  - cell value
  - cell type
  - formula
  - style
save risks:
  - style preservation
  - unknown XML nodes
  - formula namespace handling
tests needed:
  - typed value roundtrip
  - formula preservation
  - style preservation
```

This would save a huge amount of manual thinking. The verifier then checks every claim against specs, samples, source code, and tests.

### 2. AI-assisted object model design

This is now one of the most important accelerators.

For each format, AI should compare:

```text
spec structures
current parser output
current writer input
current tests
desired edit operations
```

Then produce:

```text
recommended object model
missing fields
schema mismatches
API design suggestions
preservation strategy
```

For FODS, AI would likely catch issues like:

```text
writer expects value_type
some tests use type
typed value roundtrip may be false
```

For FODT, AI can identify whether the current model is paragraph-only, heading-aware, table-aware, style-aware, etc.

This can speed up product design dramatically.

### 3. AI-generated preservation matrices

Same-format save is hard. AI can help create preservation test plans.

For each format:

```text
editable item
untouched item
expected preservation
test fixture
assertion
risk level
```

Example for FODS:

| Edit            | Must preserve                           |
| --------------- | --------------------------------------- |
| Change A1 value | B1 value, formulas, sheet names, styles |
| Change formula  | cell type, formula syntax, other cells  |
| Change style    | value, formula, row/column structure    |

This is ideal AI work because it is reasoning-heavy, repetitive, and easy to validate with tests.

### 4. AI-assisted test generation

AI should generate candidate tests for:

* parser behavior,
* writer behavior,
* save/reload behavior,
* object model editing,
* malformed files,
* security cases,
* preservation cases,
* installed-wheel smoke,
* NuGet consumer smoke.

But every AI test must be reviewed by tools and run.

The rule should be:

```text
AI may propose 50 tests.
Agent selects/implements only evidence-backed tests.
Tests become authority only after passing deterministically.
```

This can greatly increase coverage speed.

### 5. AI reviewer lanes for every sprint

Every mega-train should include AI reviewer agents that do not write code. Their job:

```text
Read plan
Read changed files
Read evidence
Find contradictions
Find overclaims
Find missing tests
Find stale docs
Find bundle problems
```

This would have caught many previous issues earlier:

* “manifest says artifacts exist but ZIP has none,”
* “final verdict still says PENDING,”
* “Phase Audit 2 did not cover all sample directories,”
* “FODS typed value can roundtrip as string.”

The reviewer lane should produce structured findings, then a deterministic verifier confirms them.

### 6. AI-assisted phase audits

The phase-by-phase audit is perfect for AI acceleration.

For each phase, AI should generate a gap matrix.

Example for Phase 2 sample provenance:

```text
format
sample files
missing provenance
missing hash
license unclear
tests using sample
risk
recommended taskcard
```

But the actual PASS/PARTIAL/FAIL must be determined by repo files and validators.

This can make phase audits much faster.

### 7. AI-assisted export-format acquisition

Your export dogfooding idea should get its own AI-accelerated train.

AI can help rank export targets:

```text
PDF
HTML
TXT
Markdown
CSV
JSON
PNG/SVG
```

For each target, AI can draft:

* spec/source options,
* complexity score,
* object model mapping,
* minimum POC scope,
* likely parser/writer strategy,
* security risks,
* test plan.

The system then chooses the safest next export formats.

My recommendation:

```text
Start with CSV/TXT/HTML/Markdown as quick wins.
Start PDF acquisition in parallel as a long train.
Do not block current POC on PDF.
```

### 8. AI-powered local retrieval / embeddings

This is one of the biggest possible accelerators, but it must be local and governed.

The project should build a local retrieval index over:

```text
spec normalized text
requirements
registry
samples provenance
source code
tests
reports
taskcards
memory
evidence summaries
```

Then agents can ask:

```text
What does the FODS spec say about cell value types?
Which tests prove formula preservation?
Which reports mention FODT writer limitations?
Which sample files contain headings?
```

Embeddings should not be authority. They are only a faster way to find the right evidence.

This would reduce wasted time a lot.

## The AI workflow I would implement

For every major lane, use this pattern:

```text
1. Retrieval pack
2. AI draft
3. Deterministic verification
4. Taskcard creation
5. Implementation
6. Tests
7. AI review
8. Evidence validator
9. Final human-readable report
```

Example:

```text
AI drafts object model gaps
→ verifier checks source/spec/tests
→ accepted gaps become taskcards
→ agent implements
→ tests run
→ AI reviewer challenges claims
→ evidence validator confirms
```

This is how we use more AI without creating more drift.

## Immediate high-impact AI trains to add

I would add these trains to the next sprint.

### Train A — AI Object Model Gap Analyzer

For FODS/FODT:

```text
Compare current parser/writer/object model against required edit/save behavior.
Find missing model fields, schema mismatches, preservation risks, and API gaps.
```

Output:

```text
reports/<RUN>/ai/object-model-gap-matrix.yaml
```

Then verifier converts valid gaps into taskcards.

### Train B — AI Preservation Test Generator

For FODS/FODT:

```text
Generate save/reload preservation tests for edited and untouched content.
```

Output:

```text
ai-drafts/preservation-tests/fods.yaml
ai-drafts/preservation-tests/fodt.yaml
```

Accepted tests become real deterministic tests.

### Train C — AI Phase Audit Assistant

For Phase Audit 2 and 3:

```text
Find missing sample provenance, missing parser requirements, and weak prototype proof.
```

AI drafts the matrix. Agent verifies it.

### Train D — AI Evidence Contradiction Reviewer

Before final bundle:

```text
Read final verdict, state, manifest, bundle metadata, package artifacts, test logs.
Find contradictions.
```

This is extremely important because our biggest failures have been closeout contradictions.

### Train E — AI Export Target Scout

Start export future now:

```text
Rank PDF/HTML/CSV/TXT/Markdown/JSON for acquisition.
Create acquisition plan for each.
Start PDF as long-running train.
```

AI drafts the plans, but registry/gates decide.

### Train F — AI Metrics/Telemetry Auditor

Every sprint must answer:

```text
Did we use AI?
Which provider?
How many calls?
How many tokens?
Was llm.professionalize.com used?
Was Agent Metrics Google Sheet updated?
Where is the ledger?
```

If there is no proof, final verdict must say:

```text
AI usage telemetry not proven
```

## About Agent Metrics Google Sheet

Right now, I would not assume the Format Factory bundles prove proper Agent Metrics posting.

Going forward, every sprint should include:

```text
reports/<RUN>/ai-usage-ledger.jsonl
reports/<RUN>/agent-metrics-posting-proof.md
reports/<RUN>/llm-provider-summary.md
```

Each AI call should record:

```text
provider
model
endpoint category
purpose
input token estimate
output token estimate
api_calls_count
whether posted to Agent Metrics
posting status
redacted payload proof
```

If using `llm.professionalize.com`, the system should post usage to the Google Sheet or clearly state why it did not.

## What is doable immediately

This is doable now:

| Idea                                 |  Difficulty |    Impact |
| ------------------------------------ | ----------: | --------: |
| AI object-model gap matrix           |      Medium | Very high |
| AI preservation test proposals       |      Medium | Very high |
| AI evidence contradiction reviewer   |  Low/Medium | Very high |
| AI phase audit assistant             |      Medium |      High |
| AI export-format scout               |         Low |      High |
| AI usage ledger                      |      Medium |      High |
| Local retrieval packs                |      Medium | Very high |
| Embeddings over specs/source/reports | Medium/High | Very high |
| Fully autonomous AI implementation   |       Risky |    Medium |

I would not start with fully autonomous implementation. I would start with **AI-assisted analysis, tests, review, and retrieval**, because that gives high speed with lower drift risk.

## Best practical strategy

Use AI in three layers.

### Layer 1: AI scouts

They explore:

```text
specs
source
tests
reports
samples
```

They produce drafts and gap lists.

### Layer 2: AI builders

They generate:

```text
test candidates
object model suggestions
API proposals
implementation sketches
```

But they do not commit authority.

### Layer 3: AI reviewers

They challenge:

```text
claims
evidence
bundles
test coverage
phase audit results
```

This balances speed and safety.

## The biggest efficiency gain

The biggest gain will come from making AI create **structured gap matrices** before implementation.

For example, instead of asking the agent to “improve FODS writer,” ask AI to produce:

```text
FODS Object Model Gap Matrix
- required feature
- current code path
- current tests
- missing behavior
- recommended implementation
- recommended test
- evidence file
- risk
```

Then the agent has a clear execution map.

This prevents the weak agent from wandering.

## My honest recommendation

The next major prompt should include a dedicated:

```text
AI ACCELERATION MEGA-TRAIN
```

with these lanes:

```text
1. Object model gap analysis
2. Preservation test generation
3. Phase audit gap extraction
4. Export-format acquisition scout
5. Evidence contradiction review
6. AI usage/metrics telemetry
7. Local retrieval/embedding readiness
```

And every AI output must follow this lifecycle:

```text
AI draft
→ evidence verification
→ taskcard
→ implementation
→ tests
→ evidence
→ accepted authority
```

## Bottom line

The fastest safe path is not to let AI “do everything.”

The fastest safe path is to let AI do what it is excellent at:

```text
read more,
compare faster,
find gaps,
draft tests,
review contradictions,
summarize evidence,
plan next steps,
generate implementation sketches.
```

Then let deterministic tools decide what is true.

If we implement this properly, the project speed can increase significantly without returning to drift.
