# Source Hygiene Audit

**Sprint/Run ID:** ff-archaeology-20260625

---

## Summary

Source hygiene is ACCEPTABLE. No build artifacts are checked into git. No generated files
are mixed with source. The `src/` tree is clean from a packaging and import perspective.
Some workspace clutter exists (nested `build/lib/` directories from local development) but
does NOT affect source inventory or CI.

---

## Build Artifact Inventory

### Nested build/lib/ Directories (Development Artifacts)

Found in `src/python/*/build/` — NOT committed to git (.gitignore excludes these).

| Package | Nesting Depth | Example Path |
|---------|--------------|-------------|
| FODS | 4 levels | `fods/build/lib/fods/build/lib/fods/build/lib/fods/` |
| ZST | 5 levels | `zst/build/lib/zst/build/lib/zst/build/lib/zst/build/lib/zst/` |
| SYLK | 3 levels | `sylk/build/lib/sylk/build/lib/sylk/` |
| ABW | 1 level | `abw/build/lib/abw/` |
| DIF | 1 level | `dif/build/lib/dif/` |
| FODT | 1 level | `fodt/build/lib/fodt/` |
| GNUMERIC | 1 level | `gnumeric/build/lib/gnumeric/` |
| ODS | 1 level | `ods/build/lib/ods/` |

**Assessment:** These are standard Python setuptools build artifacts. The deep nesting
in FODS (4 levels) and ZST (5 levels) suggests repeated `python -m build` runs without
`--clean`. These have zero impact on:
- pip install (build/ is excluded from MANIFEST.in)
- pytest discovery (conftest.py excludes build/ via testpaths)
- source inventory (Glob/Grep tools skip build/ correctly)

**Recommendation:** Run `python -m build --clean` for FODS, ZST, SYLK to eliminate nesting.
Low priority — workspace cosmetic only.

---

## Egg-Info Directories

**Count:** 20 `aspose_format_factory_*.egg-info/` directories in `src/python/`

**Assessment:** Standard pip development install artifacts. Each contains:
- `PKG-INFO` — package metadata
- `SOURCES.txt` — file manifest
- `requires.txt` — dependencies

Created by `pip install -e src/python/{format}/`. Normal development workflow.
All dated 2026-06-05 through 2026-06-24.

**Status:** ACCEPTABLE — normal development artifacts.

---

## Architecture-Only Files

### Python Spec Skeleton Files (17 identified)

Marked with `# GENERATED — architecture_only` comment at top of file.
Location: `src/python/{format}/spec/` subdirectories.

| Format | File | Status |
|--------|------|--------|
| FODT | `spec/table/table.py` | architecture_only |
| FODT | `spec/table/table_row.py` | architecture_only |
| FODT | `spec/table/table_cell.py` | architecture_only |
| FODT | `spec/text/text_body.py` | architecture_only |
| FODT | `spec/text/list_item.py` | architecture_only |
| FODS | `spec/style/style.py` | architecture_only |
| FODS | `spec/number/number_style.py` | architecture_only |
| NDJSON | `spec/record/field.py` | architecture_only (implemented) |
| XCF | `spec/layer/channel.py` | architecture_only (implemented) |
| ... (8 more) | | |

**Assessment:** These are intentional spec-parity markers generated by
`tools/spec/generate_canonical_stubs.py`. They are NOT production stubs — they are
the spec hierarchy scaffold. V48 validator blocks RELEASE_GATE items from citing
these as evidence. V48 is operational.

**NOT a hygiene problem.** Architecture-only files are by design.

### .NET Spec Skeleton Files (12 identified)

Location: `src/net/fodt/Spec/`, `src/net/fods/Spec/`

| Project | Files | Status |
|---------|-------|--------|
| FODT .NET | `Spec/Text/Paragraph.cs`, `Spec/Table/TableCell.cs`, etc. | architecture_only |
| FODS .NET | `Spec/Table/TableCell.cs`, `Spec/Office/Document.cs`, etc. | architecture_only |

Same assessment: intentional spec scaffolding, V48 enforced.

---

## Generated Files Mixed With Source?

**Finding: NO.**

Automated scan shows:
- `src/python/*/` contains only hand-maintained or agent-maintained source files
- `src/net/*/` contains only hand-maintained or agent-maintained C# files
- No Jinja2 outputs, T4 outputs, or protobuf generated files in `src/`
- Generated outputs go to `.local/`, `reports/`, `registry/` — NOT `src/`

**Exception:** `generate_canonical_stubs.py` DOES write to `src/python/{format}/spec/`.
These files are intentional architecture scaffolding (see above), explicitly marked,
and governed by V48. This is by design.

---

## Duplicate Package Outputs

**Finding: NO DUPLICATES.**

- No `src/python/{format}/{format}/` recursive nesting (unlike build/lib/)
- No duplicate .py files at different paths implementing the same class
- No conflicting package distributions

---

## __pycache__ Status

**Finding: CLEAN.**

`__pycache__/` directories are in `.gitignore` and not committed. pytest discovery
correctly handles them. No stale bytecode in source tree.

---

## Committed Temporary Files

**Finding: NONE.**

No `.tmp`, `.bak`, `.orig`, or editor swap files in `src/`.

---

## Stale Generated Code in Source

**Finding: MINOR RISK — analytics masquerade files**

The following files were classified as "analytics masquerade" in the product inventory audit:
- `src/python/gnumeric/gnumeric_workbook_stats.py` — named like analytics, is actually domain logic
- `src/python/toml/config_document.py` — named like config, is actually analytics
- 14 additional files with similar masquerade pattern

These files contain spec-backed analytical functions but are named misleadingly.
They remain in `src/` because the rename would affect 16+ import chains.
GAP-PROD-INV-MASQ-001 tracks this as deferred governance debt.

**Assessment:** Not a hygiene blocker — files are functional and governed. Rename is a
future cleanup task (low priority, high disruption).

---

## Old Artifacts Influencing Source Inventory?

**Finding: NO.**

Build artifacts are properly excluded from:
- pip install (`build/` excluded from wheel manifest)
- pytest discovery (`build/` excluded via conftest.py testpaths setting)
- Grep/Glob tools (exclude pattern correctly set)

Source inventory is NOT polluted by build artifacts.

---

## Hygiene Summary

| Check | Status | Action |
|-------|--------|--------|
| build/lib/ nesting | COSMETIC | Optional cleanup |
| .egg-info/ dirs | ACCEPTABLE | Normal dev artifacts |
| Architecture-only stubs | BY DESIGN | Governed, V48 enforced |
| Generated files in src/ | NO (by design only) | None needed |
| Duplicate outputs | NONE | None needed |
| __pycache__ committed | NO | None needed |
| Temp files | NONE | None needed |
| Analytics masquerade | DEFERRED DEBT | Low priority rename (GAP-PROD-INV-MASQ-001) |

**Overall hygiene rating: GOOD.** No blockers. Minor cosmetic cleanup optional.
