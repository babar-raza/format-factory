# Phase Verdict — Machinery Truth, Product Contract, and Architecture Documentation
**Mission:** MACHINERY-TRUTH-PRODUCT-CONTRACT-20260624
**Generated:** 2026-06-24
**HEAD:** 1852a46d

---

## VERDICT

```
MACHINERY_TRUTH_PARTIAL_PRODUCT_GRADING_MAY_PROCEED_WITH_LIMITS
```

---

## Rationale

### What Is Real and Wired

1. **SAL Pipeline** — NOT ghost infrastructure. 22 tools, 14,309 spec facts across 23 formats
   (generated 2026-06-21). Entry points: `sal_master_runner.py`, `run_spec_pipeline.py`.
   Wired into `autonomous_cycle.py` Step 0a-refresh via `refresh_check.py`.

2. **Capability Layer + Feature Compiler** — EXISTS and is CONSUMED. `capability_to_feature_compiler.py`
   is imported in `autonomous_cycle.py` at line 1481. Gap ledger (1,003 gaps; 969 closed) feeds
   work item generation. `gap_ledger_ref` injected into declaration items (Step 3a-pre).

3. **Governance** — 50 governance validators enforced every sprint cycle. Source structure
   baseline (frozen LOC caps) enforced. Product deepening gate (3/20 formats) enforced.
   TC-GUARD-001 blocks items without gap_ledger_ref. V48 blocks architecture_only stubs
   in release gate claims.

4. **Python Products** — All 20 formats installed and functional. 11 POC targets verified
   with end-to-end proof (load→inspect→edit→save→reload→export from installed package).

5. **.NET Products** — REAL source at `src/net/` (not `src/dotnet/`). FODS: 618 tests,
   all ops PASS. FODT: all ops PASS. Netpbm: POC ops PASS. G11-G sub-gate approved.

6. **Autonomous Supervision** — 172 supervisor scripts; 49 closed master plan sections;
   per-chat plan locking; adversarial contradiction detection; bounded repair; evidence
   pipeline with ZIP + SHA-256 review packages.

### Limits on Product Grading

1. **17/20 formats blocked for product deepening** — `qname_compliance_status` must reach
   `verified` in `shared/qname-registry/{format}.yaml`. Currently only abw, fods, fodt
   are verified. All other formats are gated until QName advancement.

2. **No automated QName advancement** — advancement from `seeded` → `implementing` →
   `verified` is a manual skill-guided process. No automated pipeline exists.

3. **Architecture documentation severely stale** — `docs/architecture.md` was last reviewed
   2026-05-04 (Phase 0). The system is Phase 4+. Phase 4 addendum appended 2026-06-24;
   full rewrite recommended.

4. **ZERO durable learning** — No `failure-memory.json`. Corrections are applied once and
   may recur in future sprints unless re-applied manually. All decision logic is static.

5. **LLM verifier unavailable** — Without `GPT_OSS_ENDPOINT` + `GPT_OSS_API_KEY`, grading
   of PRODUCT_SOURCE items is deferred. Sprint verdicts rely on deterministic validators.

### Full Gate 11 Remains a TRUE_EXTERNAL_GATE

G11-G sub-gate was approved by Babar Raza 2026-06-05 for FODS, FODT, Netpbm.
Full commercial release (NuGet publication, customer-readiness-checklist.md, final
commercial sign-off) requires Babar Raza's authorization. This is NOT agent-resolvable.

---

## Phase Outputs Produced

| Output | Path | Status |
|--------|------|--------|
| Current-state baseline YAML | reports/machinery-truth/baseline-20260624.yaml | WRITTEN |
| Machinery layer inventory | reports/machinery-truth/machinery-layer-inventory-20260624.md | WRITTEN |
| Product contracts (Python + .NET) | reports/machinery-truth/product-contract-20260624.md | WRITTEN |
| Claim verification matrix | reports/machinery-truth/claim-verification-matrix-20260624.md | WRITTEN |
| Stale document register | reports/machinery-truth/stale-register-20260624.md | WRITTEN |
| Architecture doc update | docs/architecture.md (Phase 4 addendum appended) | WRITTEN |
| Phase verdict | reports/machinery-truth/verdict-20260624.md | WRITTEN |

---

## Next Work (Product Grading Phase Handoff)

With MACHINERY_TRUTH established, product grading may proceed as follows:

### Immediate (no blockers)

1. **ABW, FODS, FODT** — continuation_allowed=True; product deepening can resume now.
   Suggested: advance any remaining open gaps in poc-targets.yaml for these formats.

2. **Gate 11 preparation** — agent-owned preparation (customer-readiness-checklist.md items
   C1-C20 evidence gathering). Gate 11 execution = TRUE_EXTERNAL_GATE (Babar Raza).

### With QName Gate Advancement

3. **Advance 17 formats from 'seeded'/'implementing' → 'verified'** — use `/qname-backfill`
   skill per format. Priority order: formats with most test coverage (fodt→fods already done;
   next: zst, xcf, ndjson, csv, gnumeric, ods, odt, pbm/pgm/ppm family).

4. **Wire qname_evidence.entry_count** — `registry/product-deepening-ledger.yaml` has
   `qname_evidence.entry_count=0` for all formats; needs to be populated from
   `shared/qname-registry/{format}.yaml` actual entry counts.

### Architecture Hardening

5. **Full rewrite of docs/architecture.md** — Phase 0 document needs to reflect Phase 4+
   reality. The Phase 4 addendum is a bridge; a proper rewrite would align the full
   document with src/net/, 50 validators, 22 SAL tools, 172 supervisor files, etc.

6. **spec-to-feature-radical-correction-plan.md update** — The "6 systemic failures"
   section has 3 CONTRADICTED claims. Update the plan to reflect addressed failures
   while keeping the remaining valid gaps (SUP-GAP-001, SUP-GAP-002, durable learning).

### Known Open Technical Issues

7. **GAP-XCF-LAYER-NAMES** — `xcf_layer_name_list` returns synthetic "Layer N" names,
   not real XCF layer names. Rename to `xcf_layer_synthetic_names_list` deferred due
   to 20+ test callers. Needs resolution before XCF POC claim.

8. **adversarial_check.py SSL timeout** — Test suite hits SSL network timeout waiting
   for LLM endpoint. Add mock/offline fallback to test to prevent timeout in offline CI.
