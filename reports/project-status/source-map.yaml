source_map:
  generated_at: "2026-07-02"
  mission: "sorted-sprouting-flame"

source_mapping:
  - field_id: "SM-001"
    field: "Active formats (with Python source)"
    lane: "product"
    canonical_source: "registry/format-registry.yaml"
    reader: "generate_statistics.py::_count_formats() / generate_architecture_inventory.py::_count_formats()"
    calculation: "count(formats where format_id != 'odf-shared' AND src/python/{fid}/ exists)"
    freshness_rule: "recomputed at every generation"
    missing_source_behavior: "returns 0 with graceful degradation"
    contradiction_behavior: "N/A — single source"
    validation: "assert 0 <= result <= len(all_formats)"

  - field_id: "SM-002"
    field: "Total formats in registry"
    lane: "product"
    canonical_source: "registry/format-registry.yaml"
    reader: "generate_statistics.py::_count_formats()"
    calculation: "len(data.get('formats', []))"
    freshness_rule: "recomputed at every generation"
    missing_source_behavior: "returns 0"
    contradiction_behavior: "N/A"
    validation: "assert result >= 20"

  - field_id: "SM-003"
    field: "Format families count"
    lane: "product"
    canonical_source: "registry/format-registry.yaml family field"
    reader: "generate_statistics.py::_count_formats()"
    calculation: "len(distinct family values)"
    freshness_rule: "recomputed at every generation"
    missing_source_behavior: "returns 0"
    contradiction_behavior: "N/A"
    validation: "N/A"

  - field_id: "SM-004"
    field: "Python source file count"
    lane: "product"
    canonical_source: "src/python/**/*.py"
    reader: "generate_statistics.py::_count_source()"
    calculation: "rglob('*.py') excluding __pycache__, build, egg-info"
    freshness_rule: "recomputed at every generation"
    missing_source_behavior: "returns 0 if directory missing"
    contradiction_behavior: "N/A"
    validation: "N/A"

  - field_id: "SM-005"
    field: ".NET source file count"
    lane: "product"
    canonical_source: "src/net/**/*.cs"
    reader: "generate_statistics.py::_count_source()"
    calculation: "rglob('*.cs') excluding obj, bin"
    freshness_rule: "recomputed at every generation"
    missing_source_behavior: "returns 0"
    contradiction_behavior: "N/A"
    validation: "N/A"

  - field_id: "SM-006"
    field: "Python test count"
    lane: "product"
    canonical_source: "tests/python/**/*.py"
    reader: "generate_statistics.py::_count_tests()"
    calculation: "rglob('*.py') excluding __pycache__"
    freshness_rule: "recomputed at every generation"
    missing_source_behavior: "returns 0"
    contradiction_behavior: "N/A"
    validation: "N/A"

  - field_id: "SM-007"
    field: ".NET test count"
    lane: "product"
    canonical_source: "tests/net/**/*.cs"
    reader: "generate_statistics.py::_count_tests()"
    calculation: "rglob('*.cs') excluding obj, bin"
    freshness_rule: "recomputed at every generation"
    missing_source_behavior: "returns 0"
    contradiction_behavior: "N/A"
    validation: "N/A"

  - field_id: "SM-008"
    field: "Supervisor + Certification test count"
    lane: "machinery"
    canonical_source: "tests/supervisor/**/*.py + tests/certification/**/*.py"
    reader: "generate_statistics.py::_count_tests()"
    calculation: "sum of both rglob counts"
    freshness_rule: "recomputed at every generation"
    missing_source_behavior: "returns 0 per missing dir"
    contradiction_behavior: "N/A"
    validation: "N/A"

  - field_id: "SM-009"
    field: "Governance validator count (by module)"
    lane: "machinery"
    canonical_source: "tools/supervisor/governance_validators*.py"
    reader: "generate_statistics.py::_count_governance() / generate_architecture_inventory.py::_collect_validators()"
    calculation: "glob all governance_validators*.py; count lines starting with 'def validate_'"
    freshness_rule: "recomputed at every generation"
    missing_source_behavior: "returns 0"
    contradiction_behavior: "N/A"
    validation: "assert result >= 90 (regression check)"

  - field_id: "SM-010"
    field: "Capabilities total and active"
    lane: "machinery"
    canonical_source: ".governance/capabilities/registry.yaml"
    reader: "generate_statistics.py::_count_governance() / generate_architecture_inventory.py::_collect_capabilities()"
    calculation: "len(capabilities list) for total; count(status=='active') for active"
    freshness_rule: "recomputed at every generation"
    missing_source_behavior: "returns 0/0"
    contradiction_behavior: "N/A"
    validation: "assert active <= total"

  - field_id: "SM-011"
    field: "Skill count and status breakdown"
    lane: "machinery"
    canonical_source: ".supervisor/skill-registry.yaml"
    reader: "generate_statistics.py::_count_governance() / generate_architecture_inventory.py::_collect_skills()"
    calculation: "len(skills list) for total; group by status"
    freshness_rule: "recomputed at every generation"
    missing_source_behavior: "returns 0"
    contradiction_behavior: "N/A"
    validation: "assert total >= 100"

  - field_id: "SM-012"
    field: "Command file count"
    lane: "machinery"
    canonical_source: ".claude/commands/*.md"
    reader: "generate_statistics.py::_count_governance() / generate_agent_inventory.py::_collect_commands()"
    calculation: "glob('*.md') excluding _readme.md"
    freshness_rule: "recomputed at every generation"
    missing_source_behavior: "returns 0"
    contradiction_behavior: "N/A"
    validation: "N/A"

  - field_id: "SM-013"
    field: "Oracle formats verified and pass rate"
    lane: "product"
    canonical_source: "oracle/formats/*/reports/oracle-run-summary.json"
    reader: "generate_statistics.py::_count_oracle()"
    calculation: "iterate format dirs; read oracle-run-summary.json; sum total_cases and results.PASS"
    freshness_rule: "recomputed at every generation"
    missing_source_behavior: "returns 0/0 for missing format dirs"
    contradiction_behavior: "N/A"
    validation: "assert total_pass <= total_cases"

  - field_id: "SM-014"
    field: "Certification (certified / total)"
    lane: "product"
    canonical_source: "reports/certification/portfolio-certification-matrix.json"
    reader: "generate_statistics.py::_count_certification()"
    calculation: "portfolio_summary.certified / portfolio_summary.total_formats"
    freshness_rule: "recomputed at every generation"
    missing_source_behavior: "returns 0/0"
    contradiction_behavior: "N/A"
    validation: "assert certified <= total"
    note: "denominator is 'formats with source' not 'all tracked formats' — must be labeled accordingly"

  - field_id: "SM-015"
    field: "Autonomous sprint count"
    lane: "machinery"
    canonical_source: "reports/supervisor/maturity-trend.json"
    reader: "generate_statistics.py::_count_infrastructure()"
    calculation: "data.sprint_count"
    freshness_rule: "read from committed report — may lag actual sprint count"
    missing_source_behavior: "returns 0"
    contradiction_behavior: "N/A"
    validation: "N/A"

  - field_id: "SM-016"
    field: "Avg evidence quality"
    lane: "machinery"
    canonical_source: "reports/supervisor/maturity-trend.json"
    reader: "generate_statistics.py::_count_infrastructure()"
    calculation: "data.summary.avg_evidence_quality_score"
    freshness_rule: "read from committed report"
    missing_source_behavior: "returns 0"
    contradiction_behavior: "N/A"
    validation: "assert 0.0 <= result <= 1.0"

  - field_id: "SM-017"
    field: "Evidence run count"
    lane: "machinery"
    canonical_source: ".local/evidences/"
    reader: "generate_statistics.py::_count_infrastructure()"
    calculation: "count of subdirectories in .local/evidences/"
    freshness_rule: "read at generation time — session-local state"
    missing_source_behavior: "returns 0"
    contradiction_behavior: "N/A"
    note: "GITIGNORED — count varies by machine; label as 'session-local state'"
    validation: "N/A"

  - field_id: "SM-018"
    field: "Sample file count"
    lane: "shared"
    canonical_source: "samples/by-format/*"
    reader: "generate_statistics.py::_count_infrastructure()"
    calculation: "rglob('*') excluding __pycache__"
    freshness_rule: "recomputed at every generation"
    missing_source_behavior: "returns 0"
    contradiction_behavior: "N/A"
    validation: "N/A"

  - field_id: "SM-019"
    field: "Documentation file count"
    lane: "shared"
    canonical_source: "docs/**/*.md"
    reader: "generate_statistics.py::_count_infrastructure()"
    calculation: "rglob('*.md')"
    freshness_rule: "recomputed at every generation"
    missing_source_behavior: "returns 0"
    contradiction_behavior: "N/A"
    validation: "N/A"

  - field_id: "SM-020"
    field: "Per-format oracle pass rate"
    lane: "product"
    canonical_source: "oracle/formats/{format_id}/reports/oracle-run-summary.json"
    reader: "generate_product_inventory.py::collect_product_inventory()"
    calculation: "oracle.get('pass_rate', 'N/A')"
    freshness_rule: "recomputed per format at generation time"
    missing_source_behavior: "renders 'N/A'"
    contradiction_behavior: "N/A"
    note: "N/A for formats without source should render as '— (no source)' not 'N/A' to disambiguate"
    validation: "N/A"

  - field_id: "SM-021"
    field: "Per-format certification status"
    lane: "product"
    canonical_source: "reports/certification/portfolio-certification-matrix.json"
    reader: "generate_product_inventory.py::collect_product_inventory()"
    calculation: "cert_by_fmt.get(fid, {}).get('overall_verdict', 'N/A')"
    freshness_rule: "recomputed at generation time"
    missing_source_behavior: "renders 'N/A'"
    contradiction_behavior: "N/A"
    note: "N/A for formats without source should render as '— (no source)'"
    validation: "N/A"

  - field_id: "SM-022"
    field: "Per-format gate summary"
    lane: "product"
    canonical_source: "registry/format-registry.yaml::formats[n].gates"
    reader: "generate_product_inventory.py::_gate_summary()"
    calculation: "find max gate index with status in passed/approved/commercial_readiness_in_progress"
    freshness_rule: "recomputed at generation time"
    missing_source_behavior: "renders 'N/A'"
    contradiction_behavior: "N/A"
    validation: "N/A"

  - field_id: "SM-023"
    field: "Architecture layer file counts"
    lane: "machinery"
    canonical_source: "directory paths in LAYER_DEFINITIONS constant"
    reader: "generate_architecture_inventory.py::_collect_layers()"
    calculation: "rglob('*').is_file() count per layer path"
    freshness_rule: "recomputed at generation time"
    missing_source_behavior: "returns 0"
    contradiction_behavior: "N/A"
    note: "L09 and L08 paths are gitignored local state — their counts are machine-specific and misleading"
    validation: "N/A"

  - field_id: "SM-024"
    field: "Gate pipeline (autonomous flag)"
    lane: "product"
    canonical_source: "registry/gate-contract-registry.yaml"
    reader: "generate_architecture_inventory.py::_collect_gates()"
    calculation: "gates[n].autonomous or gates[n].agent_executable"
    freshness_rule: "recomputed at generation time"
    missing_source_behavior: "returns empty list"
    contradiction_behavior: "WARN: if gate is autonomous=True but its 'approval' variant is in hard_prohibitions, emit explanatory note"
    validation: "assert len(gates) >= 10"

  - field_id: "SM-025"
    field: "Hard prohibitions list"
    lane: "machinery"
    canonical_source: ".supervisor/policies.yaml::hard_prohibitions"
    reader: "generate_agent_inventory.py::_collect_policies()"
    calculation: "data.get('hard_prohibitions', autonomous_continuation.hard_prohibitions)"
    freshness_rule: "recomputed at generation time"
    missing_source_behavior: "returns empty list"
    contradiction_behavior: "cross-check with gate registry; emit note for gate_*_approval items"
    validation: "N/A"

  - field_id: "SM-026"
    field: "Known limitation: formats without source"
    lane: "product"
    canonical_source: "registry/format-registry.yaml + src/python/ directory scan"
    reader: "generate_project_status.py::_generate_limitations()"
    calculation: "formats where format_id != 'odf-shared' AND NOT src/python/{fid}/ exists"
    freshness_rule: "recomputed at generation time"
    missing_source_behavior: "skips if yaml unavailable"
    contradiction_behavior: "N/A"
    validation: "N/A"

  - field_id: "SM-027"
    field: "Known limitation: G11 pending"
    lane: "release"
    canonical_source: "registry/format-registry.yaml::formats[n].gates.gate_11.status"
    reader: "HARDCODED in _generate_limitations() — NOT read from registry"
    calculation: "N/A (hardcoded string)"
    freshness_rule: "STALE BY DESIGN — never recomputed"
    missing_source_behavior: "N/A"
    contradiction_behavior: "N/A"
    note: "SHOULD be derived from gate_11 status in format-registry.yaml for each relevant format"
    validation: "N/A"

status_fields_without_canonical_source: 3
# SM-027 (G11 hardcoded), CL-031 (PyPI publication hardcoded), CL-033 (external users hardcoded)
