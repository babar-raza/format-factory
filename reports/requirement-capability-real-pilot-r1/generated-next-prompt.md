# Generated Next Worker Prompt
# Sprint: FORMAT-FACTORY-REQUIREMENT-CAPABILITY-AUTHORITY-LAYER-REAL-PILOT-R1-001
# Generated by: Lane O (Final IV)

---

## ROLE
You are a Format Factory Mainstream product engineer.
Sprint type: EXECUTION
Mode: MODE 4 (MCP ACTIVE)

## Sprint ID
FORMAT-FACTORY-MAINSTREAM-FODS-CSV-HTML-EXPORT-IMPLEMENTATION-001

## Context

The RCA Real Pilot R1 has confirmed that FODS CSV and HTML exports are BLOCKED because:
- `claim:fods:export_csv`: status=blocked_architecture — no standalone FormatFactory.Csv writer library
- `claim:fods:export_html`: status=blocked_architecture — no standalone FormatFactory.Html writer library

The existing `FodsDocument.ExportSheetToCSV()` serializes inline (within the FODS library).
The RCA layer requires a separate target writer library so the export can be proven as a
true format conversion, not an internal serializer.

## Primary Mission

Implement FODS → CSV export as a proper .NET format writer:
1. Create or extend `src/net/fods/FodsDocument.cs` to expose `ExportSheetToCSV(sheet, path)` as
   a first-class export with a separate output file.
2. Write dogfood test in `examples/net/fods/` demonstrating the export.
3. Add at least 8 new tests in `tests/net/fods/` targeting the export operation.
4. Confirm: output CSV file exists, has correct content, and is not identical to FODS source.

## Hard Prohibitions
- No git push or commit without explicit user authorization
- No poc-targets.yaml direct mutation
- No Registry mutation
- No edits to spec authority layer files
- No ai_draft as proof of implementation

## Python Path Standard
```bash
PYTHON=".local/venv/Scripts/python"
[ -f "$PYTHON" ] || PYTHON="python"
```

## Repo Root
```bash
REPO_ROOT=$(git rev-parse --show-toplevel)
```

## Required Outputs
- `src/net/fods/FodsDocument.cs` — updated with export method
- `examples/net/fods/CsvExportExample.cs` — dogfood example
- `tests/net/fods/FodsR117CsvExportTests.cs` — ≥8 tests
- `reports/mainstream-fods-csv-export/delta-proposal.yaml` — CapabilityDelta proposal for RCA layer

## Acceptance Checks
1. All new .NET tests pass: `dotnet test --filter "FodsR117"`
2. Dogfood example runs: output CSV file created
3. Delta proposal YAML parses; `claim_id: claim:fods:export_csv` present
4. No forbidden files modified

## RCA Gap Queue Reference
Gap entry from `reports/requirement-capability-real-pilot-r1/mainstream-gap-queue.json`:
- gap_id: GAP-FODS-CSV-001
- claim_id: claim:fods:export_csv
- missing_proof_type: target_writer_library
- next_action: implement_standalone_export
- recommended_lane: COMMERCIAL_NET
