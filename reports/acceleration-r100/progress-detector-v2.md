# Progress Detector v2 — Train H

## Enhancements
- `CAPABILITY_CATEGORIES`: 6 categories (load, edit, save, export, dogfood, package) + other
- `category_progress(matrix)` — per-category done/total counts from POC matrix
- `DONE_STATUSES` set for classification

## Tests
8 tests in `test_detect_product_progress_v2.py`
