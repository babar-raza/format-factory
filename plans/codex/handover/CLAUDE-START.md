---
artifact_id: FF6-CLAUDE-EXECUTION-START-EVENT-29
artifact_type: execution_runbook
visibility: internal
publish_allowed: false
generated_by: codex
generated_at: 2026-07-30
---

# Claude execution start — FF6 Event 29

Execution is autonomous. Do not ask for continuation. Do not infer completion
from file presence, generated counts, passing focused tests, or old status.

## 1. Reconstruct authority

Read `AGENTS.md`, `CLAUDE.md`, `plans/master-plan.md`, this packet,
`plans/strategic/ff6/product-goal.yaml`, the autonomous production plan,
controller, complete journal, XLIFF taskcard, and UBL fallback taskcard.

Run the exact preflight in [Event 29 runbook](event-29/RUNBOOK.md). A newer
valid event supersedes Event 29.

## 2. Register and own

Use Claude's ambient coordination hooks and explicitly claim the broad/logical
XLIFF Batch 005 scope. Never reuse the Codex identity. Query the registry and
routing plane, select the registered skills, create a new execution manifest,
run the mutation guard, preflight each file, and record each write.

The workspace should be clean after checkout of GitLab main. Any dirty state
is new evidence and must be attributed; it is not part of this handover.

## 3. Exact XLIFF task

Resume:

```text
TC-FF6-XLIFF-PROFILE-SURFACE-001
XLF-04
XLF-04-BATCH-005-PARTIAL-002_DISPOSITION_VERIFICATION_AND_OBLIGATION_COMPILATION
```

Use the TDD, SAL ingestion, and SAL healing skills. Work in bounded candidate
batches. For each candidate, independently inspect the pinned authority
occurrence, add discriminating evidence, repair contradictions, then update
canonical SAL/obligation ownership. Do not let the current generator act as
its own oracle.

Preserve:

- 1,130 source-authentic candidate identities;
- 105 expected obligation IDs;
- 25 existing source-bound obligation rows and stable IDs;
- fail-closed incomplete status;
- XLIFF 2.0/2.1 profile separation;
- authority digests and resource protections.

Resolve rather than hide:

- all 1,130 unverified candidate dispositions;
- 60 expected IDs with no candidate mapping;
- 80 missing source-bound obligation rows;
- any normative behavior requiring denominator expansion.

## 4. Safe fallback

Only when XLIFF exact paths are live-owned, continue UBL at
`UBL-03-PARTIAL-002` from `f98d220a`: offline import/include closure and
unique global-reference resolution. Claim only disjoint UBL paths. Keep
UBL-03 and the complete graph open.

## 5. Verification and checkpoint

Run the matrices in the Event 29 runbook. Each shift checkpoint requires:

1. bounded implementation/evidence commit pushed to GitLab main;
2. independent replay from that immutable commit;
3. one native event appended before projections;
4. matching controller/taskcard projection;
5. plan-control commit pushed to GitLab;
6. provider-neutral packet refreshed and validated twice;
7. explicit-path commit and GitLab push;
8. only the outgoing identity's leases released and session completed.

If the shift ends before a new verified boundary, leave Event 29 unchanged and
record content-addressed recovery state. Never convert partial work into a
status claim.
