---
artifact_id: FF6-CLAUDE-START-INSTRUCTION-001
artifact_type: execution_prompt
visibility: internal
publish_allowed: false
generated_by: codex
generated_at: 2026-07-29
authoritative_state: false
---

# Claude Start Instruction

MODE: EXECUTION MODE.

Continue mission `FF6-PRODUCTION-LIBRARIES-001` autonomously from the canonical
GitLab-main checkpoint. Do not ask for confirmation or continuation.

First read, in order:

1. `AGENTS.md`
2. `plans/codex/handover/START-HERE.md`
3. `plans/strategic/ff6/product-goal.yaml`
4. `plans/strategic/autonomous-six-python-production-execution-plan.md`
5. `plans/strategic/ff6/controller-state.yaml`
6. `plans/strategic/ff6/events.jsonl`
7. `plans/strategic/ff6/current-state.yaml`
8. `taskcards/TC-FF6-PROGRAM-CAPABILITIES-001.md`
9. `taskcards/TC-FF6-AUTHORITY-CLOSURE-001.md`
10. `plans/codex/handover/ACTIVE-WORK-CHECKPOINT.md`
11. `plans/strategic/ff6/capability-manifest.json`
12. every remaining file linked from `START-HERE.md`

Revalidate GitLab main, the clean worktree, LF-normalized digests, controller
event chain, current-state consistency, plan-control tests, coordination
ownership, and the known global plan-control contradiction.

Verify that `2129ad278c5d7a8b7f81559388489e6231def550` is an ancestor of
`origin/main` and that the event chain ends at event 14 with hash
`399a5069b3c843d1b4f668a8f7abeb0deffe40a234a584f6c9f7b5b3a3e70fc8`.
Use a clean checkout of GitLab main (a detached worktree at that commit is
allowed; a provider branch is not). Register and claim the active task
`TC-FF6-AUTHORITY-CLOSURE-001`; its parent remains
`TC-FF6-PROGRAM-CAPABILITIES-001` in `NEEDS_REPAIR`.

Do not restart the active task. Preserve the tested event-14 implementation:
three authority modules, one schema, four internal requirement documents, and
six focused tests. Compare their digests to the event before writing.

Resume at the first unchecked substep: complete the immutable source/legal
matrix beginning with NRRD, reverify the recorded source candidates, then
create the canonical 15-source authority lock. Continue through the exact
sections B-I in `ACTIVE-WORK-CHECKPOINT.md`.

The promoting baseline remains 11 `MISSING`, 4 `UNDECLARED`, 0 `MATCH`.
Do not modify product source, use `--allow-blocked-authority` for a promoting
run, accept a mismatched digest, commit unlicensed specification bytes, create
a provider branch, push to GitHub, or promote a product. Use registered
skills, preflight every write, record every write, stage only explicit files,
and integrate only validated work to GitLab `main`.

Do not trust `tools.plan_control next` for FF6 until its stale branch checkpoint
and task-authority split are repaired and regression-tested. The FF6 controller
and journal select the next task.

At your token or shift boundary, follow
`plans/codex/handover/SHIFT-AND-RESUME-PROTOCOL.md`. Leave either a complete,
remote-verified checkpoint or a truthful governed partial checkpoint. Never
leave required state only in chat, ignored files, or an unpushed commit.

Before transferring back to Codex, refresh this packet from live evidence,
append a checkpoint event, push the clean checkpoint to GitLab main, complete
your coordination session, and make the first unchecked substep explicit.

Continue safe unblocked work automatically. A failure in one format does not
stop the other five. Do not self-approve publication or business gates.
