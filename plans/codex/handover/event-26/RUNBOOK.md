---
visibility: internal
generated_by: codex
---

# Event 26 Resume Runbook

1. Fetch GitLab and require `15ab7d04` on `origin/main`.
2. Run `.\.venv\Scripts\python.exe plans/codex/handover/validate_handover.py --self-test`.
3. Validate event 26, controller/taskcard agreement, all batch-004 receipts,
   5/5 XLIFF authorities, and candidate check mode.
4. Register a new coordination identity and claim exact paths. Take over only
   stale leases with an audited reason.
5. Resume `XLF-04-BATCH-005` with RED tests for forged occurrence text,
   requirement/member/source digests, occurrence location, candidate digest,
   and candidate class.
6. Add explicit prose, XSD declaration/type/facet/cardinality/order/wildcard,
   and Schematron assert/report classes. Make candidate content digests bind
   every occurrence while preserving stable candidate identity.
7. Classify non-modal prose, replace all 78 coarse dispositions with exact
   mappings or reasoned non-obligations, and add authority-required denominator
   IDs and source-bound obligation rows.
8. Retain `complete=false` until zero expected IDs are unresolved and canonical
   SAL verification is complete.
9. Run focused, regression, static, authority, tamper, check-mode, and
   three-run determinism gates. Commit, journal, refresh the next versioned
   packet, push GitLab main, verify remote, and release only your own leases.

Do not mutate product source, gates, promotion, release state, GitHub, branches,
or historical event packets in this contract-stage cycle.
