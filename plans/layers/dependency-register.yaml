schema_version: "1.0"
repository_revision: "a7744cf6"
generated_at: "2026-06-26T00:00:00Z"

# Layer Dependency Graph
# Format: producer_layer_id → consumer_layer_id
# Dependency means: producer must reach target maturity before consumer can operate at target

dependencies:
  # SPEC-FACT CHAIN: L01 → L02 → L03 → L14 → L06
  - dependency_id: DEP-001
    producer_layer: L01
    consumer_layer: L02
    dependency_type: UPSTREAM_DATA
    description: "QName registry entries require spec facts to assign canonical class names"
    strength: REQUIRED
    current_unmet: true
    notes: "L01 only covers 6/20 formats; L02 has 99.4% coverage via manual entry workaround"

  - dependency_id: DEP-002
    producer_layer: L01
    consumer_layer: L03
    dependency_type: UPSTREAM_DATA
    description: "Capability records cite SAL fact IDs; without facts, capabilities are unanchored"
    strength: REQUIRED
    current_unmet: true
    notes: "1,242 capabilities exist; most cite exception_classification rather than spec_fact_refs"

  - dependency_id: DEP-003
    producer_layer: L02
    consumer_layer: L03
    dependency_type: UPSTREAM_DATA
    description: "Capability compiler uses QName entries to generate canonical class mappings"
    strength: REQUIRED
    current_unmet: false

  - dependency_id: DEP-004
    producer_layer: L03
    consumer_layer: L14
    dependency_type: UPSTREAM_DATA
    description: "Feature compiler reads gap-ledger.json from capability layer"
    strength: REQUIRED
    current_unmet: true
    notes: "L14 (feature compiler) does not exist yet — SKILL-GAP-003"

  - dependency_id: DEP-005
    producer_layer: L14
    consumer_layer: L06
    dependency_type: UPSTREAM_DATA
    description: "Feature specs from compiler drive product source generation"
    strength: ADVISORY
    current_unmet: true
    notes: "Currently product deepening uses hardcoded _EXPANSION_GOALS instead"

  # PRODUCT CHAIN: L06 → L05, L07, L08, L16, L19
  - dependency_id: DEP-006
    producer_layer: L06
    consumer_layer: L05
    dependency_type: UPSTREAM_PRODUCT
    description: "Oracle tests product API; product must exist before oracle can verify"
    strength: REQUIRED
    current_unmet: false

  - dependency_id: DEP-007
    producer_layer: L06
    consumer_layer: L07
    dependency_type: UPSTREAM_PRODUCT
    description: "Tests exercise product source; product must exist before tests can pass"
    strength: REQUIRED
    current_unmet: false

  - dependency_id: DEP-008
    producer_layer: L06
    consumer_layer: L16
    dependency_type: UPSTREAM_PRODUCT
    description: "Dogfood exports require product packages to be importable"
    strength: REQUIRED
    current_unmet: false

  - dependency_id: DEP-009
    producer_layer: L06
    consumer_layer: L19
    dependency_type: UPSTREAM_PRODUCT
    description: "Consumer API is the public surface of product packages"
    strength: REQUIRED
    current_unmet: false

  # CORPUS CHAIN: L04 → L05, L07
  - dependency_id: DEP-010
    producer_layer: L04
    consumer_layer: L05
    dependency_type: UPSTREAM_DATA
    description: "Oracle test cases use samples from corpus"
    strength: REQUIRED
    current_unmet: false
    notes: "Corpus exists but has no governance; oracle uses it anyway"

  - dependency_id: DEP-011
    producer_layer: L04
    consumer_layer: L07
    dependency_type: UPSTREAM_DATA
    description: "Roundtrip tests use sample files from corpus"
    strength: ADVISORY
    current_unmet: false

  # GOVERNANCE CHAIN: L12 ← all layers
  - dependency_id: DEP-012
    producer_layer: L12
    consumer_layer: L06
    dependency_type: ENFORCEMENT
    description: "Governance validators block product source that violates production standard"
    strength: REQUIRED
    current_unmet: false

  - dependency_id: DEP-013
    producer_layer: L12
    consumer_layer: L07
    dependency_type: ENFORCEMENT
    description: "Validators enforce test coverage and spec_qname in test files"
    strength: REQUIRED
    current_unmet: false

  - dependency_id: DEP-014
    producer_layer: L13
    consumer_layer: L06
    dependency_type: EXECUTION_CONTROL
    description: "All product source changes must go through registered skills"
    strength: REQUIRED
    current_unmet: false
    notes: "V46 (skill_transcript_present) is WARN-only; full enforcement pending"

  # SUPERVISOR CHAIN: L08, L09, L10, L12, L13 → L11
  - dependency_id: DEP-015
    producer_layer: L08
    consumer_layer: L11
    dependency_type: UPSTREAM_DATA
    description: "Supervisor pipeline consumes evidence declarations"
    strength: REQUIRED
    current_unmet: false

  - dependency_id: DEP-016
    producer_layer: L09
    consumer_layer: L11
    dependency_type: UPSTREAM_DATA
    description: "Supervisor reads continuation state to determine CONTINUE/STOP"
    strength: REQUIRED
    current_unmet: false

  - dependency_id: DEP-017
    producer_layer: L10
    consumer_layer: L11
    dependency_type: UPSTREAM_DATA
    description: "Supervisor reads plan authority files for next-sprint context"
    strength: REQUIRED
    current_unmet: false

  # RELEASE CHAIN: L06, L17, L21 → L18 → external
  - dependency_id: DEP-018
    producer_layer: L17
    consumer_layer: L18
    dependency_type: QUALITY_GATE
    description: "Package release requires regression compatibility sign-off"
    strength: REQUIRED
    current_unmet: true
    notes: "L17 layer not yet assessed"

  - dependency_id: DEP-019
    producer_layer: L21
    consumer_layer: L18
    dependency_type: GATE_APPROVAL
    description: "Package release requires Gate 11 execution approval (Babar Raza)"
    strength: REQUIRED
    current_unmet: true
    notes: "BLOCKED_EXTERNAL: Gate 11 G11-G not yet executed"

  # LAYER CONTROL PLANE: this plan bootstraps layer governance itself
  - dependency_id: DEP-020
    producer_layer: L13
    consumer_layer: L11
    dependency_type: EXECUTION_CONTROL
    description: "Supervisor work selection must route through layer control plane (plans/layers/index.yaml)"
    strength: ADVISORY
    current_unmet: true
    notes: "Currently supervisor reads next-sprint.md; layer index not yet consumed"

# Execution waves derived from dependencies
execution_waves:
  wave_1_system_healing:
    description: "System infrastructure and spec pipeline"
    layers: [L01, L02, L03, L04, L12, L13]
    rationale: "These can run in parallel; L01/L02/L03 are the primary chain"

  wave_2_product:
    description: "Product source and oracle"
    layers: [L05, L06, L07, L14]
    rationale: "L14 compiler needed for L06 spec-parity; L05/L07 validate L06"
    blocked_by: [L01, L03]  # DEP-001, DEP-004

  wave_3_release:
    description: "Release pipeline"
    layers: [L15, L16, L17, L18, L19]
    rationale: "These layers operationalize the output of L06"
    blocked_by: [L06]  # DEP-006 through DEP-009

  wave_4_governance:
    description: "Cross-cutting governance"
    layers: [L08, L09, L10, L11, L20, L21, L22, L23, L24, L25, L26, L27]
    rationale: "These can run in parallel with all waves"
