taskcard_id: TC-GAP-CONV-003
title: Re-prove the 6 formats whose install-proof went STALE (ipynb, mtlx, nrrd, safetensors, ubl, xliff)
source_issue_ids: [GAP-CONV-003]
source_issue_level: L1_EXECUTION
source_audit_finding: stage1-issue-model.json#issues[2]
why_it_matters: >
  V226 currently FAILs live because concurrent agent work changed these 6 formats'
  source after the 2026-07-15T17:36:29Z proof run. The fleet cannot be honestly
  reaffirmed "all green" until proof is refreshed against current source.
risk_addressed: closure claim decaying silently as source drifts after proof
status: completed_verified
lane_owner: agent_owned
supervisor_role: convergence_controller
required_implementation: "python tools/run_package_install_proof.py --format <fmt> for each of the 6 formats"
required_verification: Live re-invocation of validate_package_install_proof_coverage returns no STALE for these 6 formats
required_evidence: Updated reports/package-install-proof/proof-manifest.json entries with fresh proved_at/source_digest/wheel_sha256; live validator result
quality_dimensions: [implementation_correctness, integration_completeness, repeatability, idempotency]
acceptance_criteria:
  - All 6 formats show verdict PASS with source_digest matching current src/python/<fmt>/ content
  - Live V226 invocation no longer lists these formats under STALE
dependencies: []
closeout_rules: Close when live V226 re-check is clean for these 6 formats
machine_state: pending
validation_commands:
  - "python tools/run_package_install_proof.py --format ipynb --format mtlx --format nrrd --format safetensors --format ubl --format xliff"
